--- a/src/drivers/main.cpp
+++ b/src/drivers/main.cpp
@@ -1135,0 +1136,8 @@
+    const char *theron_autoload_state = getenv("FIRESTAFF_THERON_AUTOLOAD_STATE");
+    if(theron_autoload_state && *theron_autoload_state)
+    {
+        if(!MDFNI_LoadState(theron_autoload_state, NULL))
+            MDFN_printf("Theron autoload state failed: %s", theron_autoload_state);
+        else
+            MDFN_printf("Theron autoload state loaded: %s", theron_autoload_state);
+    }
