--- src/ss/vdp2.cpp
+++ src/ss/vdp2.cpp
@@ -774,4 +774,5 @@
  if(IsWrite)
  {
   const unsigned mask = (sizeof(T) == 2) ? 0xFFFF : (0xFF00 >> ((A & 1) << 3));
+  FirestaffTraceVdp2Write("vram", A, sizeof(T), *DB);
   VRAM[vri] = (VRAM[vri] &~ mask) | (*DB & mask);
