--- a/src/cdrom/scsicd.cpp
+++ b/src/cdrom/scsicd.cpp
@@ -70,0 +71,39 @@
+
+struct TheronSCSIDataOrigin
+{
+ uint32 lba;
+ uint16 offset;
+ uint64 sequence;
+};
+static TheronSCSIDataOrigin TheronSCSIDataOrigins[65536];
+static uint32 TheronSCSIDataOriginRead;
+static uint32 TheronSCSIDataOriginWrite;
+static uint32 TheronSCSIDataOriginCount;
+static uint64 TheronSCSIDataOriginSequence;
+static TheronSCSIDataOrigin TheronSCSILastDataOrigin;
+static bool TheronSCSILastDataOriginValid;
+
+static void TheronSCSIQueueDataOrigin(uint32 lba, uint16 offset, uint32 length)
+{
+ for(uint32 i = 0; i < length; i++)
+ {
+  if(TheronSCSIDataOriginCount >= 65536)
+   break;
+  TheronSCSIDataOrigins[TheronSCSIDataOriginWrite] =
+      { lba, (uint16)(offset + i), TheronSCSIDataOriginSequence++ };
+  TheronSCSIDataOriginWrite = (TheronSCSIDataOriginWrite + 1) & 0xffff;
+  TheronSCSIDataOriginCount++;
+ }
+}
+
+bool SCSICD_GetLastDataOrigin(uint32 *source_lba, uint16 *source_offset,
+                              uint64 *fifo_sequence)
+{
+ if(!TheronSCSILastDataOriginValid)
+  return false;
+ *source_lba = TheronSCSILastDataOrigin.lba;
+ *source_offset = TheronSCSILastDataOrigin.offset;
+ *fifo_sequence = TheronSCSILastDataOrigin.sequence;
+ return true;
+}
+
@@ -2882,0 +2922,2 @@
+     {
+      TheronSCSIQueueDataOrigin(SectorAddr, 24, 2048);
@@ -2883,0 +2925 @@
+     }
@@ -2884,0 +2927,2 @@
+     {
+      TheronSCSIQueueDataOrigin(SectorAddr, 16, 2048);
@@ -2885,0 +2930 @@
+     }
@@ -3148,0 +3194,9 @@
+      if(TheronSCSIDataOriginCount)
+      {
+       TheronSCSILastDataOrigin = TheronSCSIDataOrigins[TheronSCSIDataOriginRead];
+       TheronSCSIDataOriginRead = (TheronSCSIDataOriginRead + 1) & 0xffff;
+       TheronSCSIDataOriginCount--;
+       TheronSCSILastDataOriginValid = true;
+      }
+      else
+       TheronSCSILastDataOriginValid = false;
--- a/src/cdrom/scsicd.h
+++ b/src/cdrom/scsicd.h
@@ -100,0 +101,2 @@
+bool SCSICD_GetLastDataOrigin(uint32 *source_lba, uint16 *source_offset,
+                              uint64 *fifo_sequence);
--- a/src/pce/pce.cpp
+++ b/src/pce/pce.cpp
@@ -114 +114,3 @@
-extern bool TheronPCECDTraceTakeDataRead(uint16 *reader_pc, uint32 *reader_physical_pc, uint8 *value);
+extern bool TheronPCECDTraceTakeDataRead(uint16 *reader_pc, uint32 *reader_physical_pc,
+                                         uint8 *value, uint32 *source_lba,
+                                         uint16 *source_offset, uint64 *fifo_sequence);
@@ -124,0 +127,3 @@
+ uint32 source_lba;
+ uint16 source_offset;
+ uint64 fifo_sequence;
@@ -126 +131,2 @@
- if(!TheronPCECDTraceTakeDataRead(&reader_pc, &reader_physical_pc, &read_value) || trace_count >= 256)
+ if(!TheronPCECDTraceTakeDataRead(&reader_pc, &reader_physical_pc, &read_value,
+                                  &source_lba, &source_offset, &fifo_sequence) || trace_count >= 256)
@@ -145 +151,2 @@
-         "pce_cd_data_destination_candidate reader_pc=%04x reader_physical_pc=%06x writer_pc=%04x writer_physical_pc=%06x logical_destination=%04x physical=%08x read_value=%02x stored_value=%02x\n",
+         "pce_cd_origin_ram_receipt source_lba=%u source_offset=%u fifo_sequence=%llu reader_pc=%04x reader_physical_pc=%06x writer_pc=%04x writer_physical_pc=%06x logical_destination=%04x physical=%08x read_value=%02x stored_value=%02x\n",
+         source_lba, source_offset, (unsigned long long)fifo_sequence,
--- a/src/pce/pcecd.cpp
+++ b/src/pce/pcecd.cpp
@@ -104,0 +105,4 @@
+static bool TheronPCECDDataReadHasOrigin;
+static uint32 TheronPCECDDataReadLBA;
+static uint16 TheronPCECDDataReadOffset;
+static uint64 TheronPCECDDataReadFifoSequence;
@@ -180 +184,3 @@
-bool TheronPCECDTraceTakeDataRead(uint16 *reader_pc, uint32 *reader_physical_pc, uint8 *value)
+bool TheronPCECDTraceTakeDataRead(uint16 *reader_pc, uint32 *reader_physical_pc,
+                                  uint8 *value, uint32 *source_lba,
+                                  uint16 *source_offset, uint64 *fifo_sequence)
@@ -188 +194,4 @@
- return true;
+ *source_lba = TheronPCECDDataReadLBA;
+ *source_offset = TheronPCECDDataReadOffset;
+ *fifo_sequence = TheronPCECDDataReadFifoSequence;
+ return TheronPCECDDataReadHasOrigin;
@@ -722,0 +732,3 @@
+     TheronPCECDDataReadHasOrigin = SCSICD_GetLastDataOrigin(
+         &TheronPCECDDataReadLBA, &TheronPCECDDataReadOffset,
+         &TheronPCECDDataReadFifoSequence);
