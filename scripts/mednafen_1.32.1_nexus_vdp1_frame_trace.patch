--- src/ss/vdp1.h
+++ src/ss/vdp1.h
@@ -52,1 +52,2 @@
-void FirestaffCaptureRaw(void *file);
+void FirestaffCaptureRaw(void *file);
+void FirestaffTraceVramWriteFrame(unsigned frame);
--- src/ss/vdp1.cpp
+++ src/ss/vdp1.cpp
@@ -1293,2 +1293,16 @@
-static void FirestaffTraceVramWriterCode(uint32 address)
-{
+void FirestaffTraceVramWriteFrame(unsigned frame)
+{
+ const char *path = getenv("FIRESTAFF_NEXUS_TRACE_VDP1_WRITES");
+ if(!path || !*path) return;
+ if(!FirestaffVramWriteTraceFile)
+ {
+  FirestaffVramWriteTraceFile = fopen(path, "wb");
+  if(!FirestaffVramWriteTraceFile) return;
+  fputs("FIRESTAFF_NEXUS_VDP1_VRAM_WRITE_TRACE_V2\n", FirestaffVramWriteTraceFile);
+ }
+ fprintf(FirestaffVramWriteTraceFile, "frame=%u\n", frame);
+ fflush(FirestaffVramWriteTraceFile);
+}
+
+static void FirestaffTraceVramWriterCode(uint32 address)
+{
