diff --git a/src/cdrom/scsicd.cpp b/src/cdrom/scsicd.cpp
--- a/src/cdrom/scsicd.cpp
+++ b/src/cdrom/scsicd.cpp
@@ -120,3 +120,8 @@ static bool TheronTraceReadRawSector(uint8 *buffer, int32 lba)
  return ok;
 }
+
+unsigned TheronSCSITraceCurrentReadGeneration(void)
+{
+ return TheronSCSIReadCommandGeneration;
+}
 // Internal operation to the SCSI CD unit.  Only pass 1 or 0 to these macros!
diff --git a/src/pce/pcecd.cpp b/src/pce/pcecd.cpp
--- a/src/pce/pcecd.cpp
+++ b/src/pce/pcecd.cpp
@@ -90,5 +90,7 @@ using namespace Mednafen;
 
 #include "pcecd.h"
 
+namespace Mednafen { unsigned TheronSCSITraceCurrentReadGeneration(void); }
+
 namespace MDFN_IEN_PCE
 {
@@ -594,12 +595,13 @@ MDFN_FASTCALL uint8 PCECD_Read(uint32 timestamp, uint32 A, int32 &next_event, co
    case 0x1:
     ret = SCSICD_GetDB();
     if(!PeekMode && SCSICD_GetIO() && !SCSICD_GetCD())
     {
      TheronPCECDDataReadPending = true;
      TheronPCECDDataReadPC = HuCPU.GetRegister(HuC6280::GSREG_PC);
      TheronPCECDDataReadValue = ret;
-     TheronTracePCECDState("pce_cd_data_read cpu_pc=%04x data=%02x\n",
-                           TheronPCECDDataReadPC, ret);
+     TheronTracePCECDState("pce_cd_data_read cpu_pc=%04x data=%02x scsi_generation=%u\n",
+                           TheronPCECDDataReadPC, ret,
+                           Mednafen::TheronSCSITraceCurrentReadGeneration());
     }
 	    break;
