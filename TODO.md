# Firestaff TODO - Open Work

This file tracks remaining work only. Completed work belongs in `DONE.md`.

- ✅ 2026-06-16 Per-game V2 settings in M12 menu config: `M12_Config` + `M12_MenuSettingsState` extended with `csbV2*` and `theronV2*` fields (scalePercent, bilinearEnabled, crtScanlinesEnabled, crtScanlineStrength, paletteCorrectionEnabled, ditherCleanupEnabled — mirrors the existing `dm1V2*` pattern). Defaults 200% scale, 0 bilinear, 0 scanlines, 35 strength, 0 palette, 0 dither. Round-tripped through `M12_Config_SetDefaults` + the text Load + the text Save + the JSON Export + the JSON Import. New bridge modules `csb_v2_settings_pc34` + `theron_v2_settings_pc34` mirror `dm1_v2_settings_pc34`: `CSB_V2_Settings` / `Theron_V2_Settings` struct, `_from_m12_config` / `_apply_to_m12_config` / `_apply_to_runtime` (pushes scale + bilinear into the respective `_upscale_init`). CTEST: `test_csb_v2_settings_pc34` 23/23, `test_theron_v2_settings_pc34` 23/23. Probes: `firestaff_csb_v2_settings_probe` 13/13, `firestaff_theron_v2_settings_probe` 12/12. Source-locked against include/dm1_v2_settings_pc34.h, include/csb_v2_texture_upscale_pc34.h, include/csb_v22_shapes.h, include/theron_v2_texture_upscale_pc34.h, include/theron_v22_shapes.h, include/config_m12.h, THQUEST.ASM T400/T520/T600, HuC6260/HuC6270 VDC/VCE. **Wire-up done:** `M11_GameView_OpenSelectedMenuEntry` in src/engine/m11_game_view.c now reads `menuState->settings.csbV2*` / `theronV2*` and calls `csb_v2_settings_apply_to_runtime()` / `theron_v2_settings_apply_to_runtime()` right before `M11_GameView_Start`, so the saved scale + bilinear reach the live V2 runtime. New headless probe `firestaff_m12_v2_settings_wire_up_probe` 16/16: scale 100/200/400 → runtime 1/2/4 for both CSB + Theron, bilinear 0/1 round-trips, invalid values clamp, CSB + Theron globals are independent. DM1 V2 settings already had their own dispatch via `M11_Render_Set*` inside `M11_GameView_Start`.
- 🔧 2026-06-16 V2 presentation-mode pass: dm1_v2_presentation_mode_pc34 + csb_v2_presentation_mode_pc34 modules wired to M11_GameView_Start (spec->presentationMode is now pushed into the per-game V2 runtime for gameId=dm1 and gameId=csb). v2_settings_apply_v20_defaults (V2.0 filtered), v2_settings_apply_v22_defaults (V2.2 modern) added alongside the existing v2_settings_apply_v21_defaults. dm1_v22_shapes.c now explicitly included in the firestaff_v2 lib so the V22 entry branch resolves m11_v22_shapes_init. New CTEST targets: test_dm1_v2_presentation_mode_pc34 (50/50), test_csb_v2_presentation_mode_pc34 (36/36). New headless CI probes: firestaff_dm1_v2_presentation_mode_probe (30/30), firestaff_csb_v2_presentation_mode_probe (27/27). 23/23 Phase A invariants still pass. The 166/166 green set confirms the M12_PRESENTATION_V1_ORIGINAL / V20_FILTERED / V21_UPSCALED / V22_MODERN enum now reaches the per-game V2 runtime; remaining work is the actual CSB V2.1 upscale + CSB V2.2 modern asset pack authoring (the selection API is ready, the per-mode V2 modules are still TODO for CSB). Phase 8 m11_v22_shape_for_cell() runtime use from the V22 active branch is also now done (see the Phase 8 entry below for the per-frame V22 shape cache details).
- ✅ 2026-06-16 CSB V2.1 + V2.2 follow-up: csb_v2_texture_upscale_pc34 (mirror of dm1_v2_texture_upscale with csb_ prefix, 9-square viewport + panel helpers), csb_v22_shapes (9-square shape book with CSB-only PRISON_DOOR/CHAOS_RUNE/DSA_SCROLL/LORD_ORDER), dm1_v2_shape_runtime (V2.2 shape dispatch wrapper around m11_v22_shape_for_cell, with M11_GameView_Start hook). CTEST: test_csb_v2_texture_upscale_pc34 30/30, test_csb_v22_shapes_pc34 54/54, test_dm1_v2_shape_runtime_pc34 46/46. Probes: firestaff_csb_v2_texture_upscale_probe 13/13, firestaff_csb_v22_shapes_probe 21/21, firestaff_dm1_v2_shape_runtime_probe 12/12. The CSB V2.1 upscale + CSB V2.2 shape modules are now wired into csb_v2_presentation_mode_set() so they activate when the user picks CSB V2.1 or V2.2. **V2.0 filter config also done (this pass):** new `csb_v2_filter_config_pc34` module stores the per-frame filter toggles (crtScanlinesEnabled, crtScanlineStrength, paletteCorrectionEnabled, ditherCleanupEnabled) as a module-level global; `csb_v2_settings_apply_to_runtime` now also calls `csb_v2_filter_config_apply()` so the M11 launch wire-up pushes both upscale AND filter config in a single call. CTEST: test_csb_v2_filter_config_pc34 26/26. Probe: firestaff_csb_v2_filter_config_probe 21/21. Remaining work: actual CSB modern asset pack authoring at ~/.firestaff/assets/csb/modern/ + GPU renderer integration (the runtime dispatch is in place but the renderer is a follow-up).
- 🔧 2026-06-16 Theron V1 wire-up: 3 previously-unwired V1 tests (theron_v1_direct_launch_test, theron_v1_launcher_scan_reuse_test, theron_v1_m11_direct_launch_test) + 2 unwired probes (firestaff_theron_v1_viewport_renderer_probe, firestaff_theron_v1_tile_renderer_probe) are now in ctest + the linker, all green. firestaff_theron_v1_track02_bank_probe also wired. THERON_SOURCES glob extended to also match src/theron/theron_v2_*.c (otherwise theron_v2_presentation_mode would be a dangling include). M12 lib now compiles with FIRESTAFF_ASSET_STATUS_TESTING=1 so the launcher_scan_reuse test's M12_AssetStatus_Test* symbols are in the .a. All 9 Theron V1 tests + 5 Theron V1 probes pass.
- ✅ 2026-06-16 Theron V2 presentation-mode selection: theron_v2_presentation_mode_pc34 module (include + src) maps M12_PRESENTATION_V1/V20/V21/V22 onto the Theron V2 presentation runtime. M11_GameView_Start extended to call theron_v2_presentation_mode_set_m12() when gameId=theron. Three independent presentation-mode globals (DM1/CSB/Theron) verified. CTEST: test_theron_v2_presentation_mode_pc34 40/40, firestaff_theron_v2_presentation_mode_probe 23/23. **Follow-up (now done):** Theron V2.1 upscale module (theron_v2_texture_upscale_pc34 mirror of csb_v2_texture_upscale for HuC6260 256x224 NTSC base, with NTSC fullscreen + 192x160 dungeon-viewport helpers), Theron V2.2 shape book (theron_v22_shapes 4×3 layout with Theron-only TELEPORTER/ALARM/SECRET_DOOR/FLOODED/LIT_TORCH shapes), both wired into theron_v2_presentation_mode_set() so they activate on V2.1/V2.2 selection. CTEST: test_theron_v2_texture_upscale_pc34 25/25, test_theron_v22_shapes_pc34 62/62. Probes: firestaff_theron_v2_texture_upscale_probe 14/14, firestaff_theron_v22_shapes_probe 26/26. **V2.0 filter config also done (this pass):** new `theron_v2_filter_config_pc34` module parallels the CSB filter config for the PC Engine CD (HuC6260 VDC + HuC6270 VCE) Theron pipeline. CTEST: test_theron_v2_filter_config_pc34 24/24. Probe: firestaff_theron_v2_filter_config_probe 18/18. Remaining work: actual Theron modern asset pack at ~/.firestaff/assets/theron/modern/ + DM1 V2.2 actual GPU render path (the dispatch hook is in place via dm1_v2_shape_runtime_v22_active()).
- ✅ 2026-06-18 csb_v2_texture_upscale test-bug (RESOLVED + pushed): `t_epx_2x` (and downstream 9square/panel/V22 EPX checks) expected nearest-2x output from `csb_v2_upscale_epx()` but the actual EPX rule returns P when neighbour conditions are not met. Fixed in two ways: (1) `t_epx_2x` documents the P-fallback and expects the column-stripe nearest output (row 0 = src col 0, row 1 = src col 0 still, since the 2x2 P=src[0]/src[1] drives both y-stride rows). (2) `t_9square_viewport`, `t_panel`, and `t_present_mode_v22_triggers_epx` use a `memset(epx_buf, 0xCC, ...)` sentinel so the EPX-wrote-it check is `epx_buf[0] != 0xCC` (not `!= 0`, which was always false when src[0] = 0). Same approach applied to the `firestaff_csb_v2_texture_upscale_probe` and the new Theron V2.1 probe/test. Ctest `csb_v2_texture_upscale` 30/30, probe 13/13, Theron V2.1 25/25. **DM1 V2.1 EPX audit (now done):** the DM1 equivalent (`dm1_v2_asset_epx_upscale` in `src/dm1v2/dm1_v2_asset_pipeline_pc34.c:205` and `v2_upscale_epx` in `src/dm1v2/dm1_v2_texture_upscale_pc34.c:96`) shares the EPX rule. The DM1 tests `test_epx_single_pixel` in `probes/verify_pass648_dm1_v2_asset_pipeline.c:55-148` and the corresponding test_dm1_v22_asset_pipeline asserts already correctly account for the P-fallback (uniform-field → all P, corner pattern with predicted top-left=64/bot-right=192, 4x4 diagonal, checkerboard = all P). `pass648_dm1_v2_asset_pipeline_probe` PASS, `test_dm1_v22_asset_pipeline` PASS, `test_dm1_v22_verification` PASS. No DM1 EPX test changes needed.

## Legend

- ❌ Not started
- 🔧 In progress / partial
- 🐛 Known bug

## Dungeon Master (DM1)

### DM1 V1

- 🔧 Original DOS capture parity: five specific paired evidence sets are blocked. Details and honest status labels at `docs/parity/DM1_V1_CAPTURE_GAP_EVIDENCE.md`. Minimum runbook at `docs/parity/DM1_V1_ORIGINAL_CAPTURE_RUNBOOK.md`.
  - Viewport: pass1052 now has a clean original DM1 PC 3.4 DOSBox turn-cycle capture with 2 `dungeon_gameplay` frames, 0 duplicate raw hashes, and pass80 classifier PASS. pass1056 now gates the pass1054 Firestaff nearest-neighbor pairing artifacts in CTest: 4 original crops present, 4 pair rows present, and the one promoted wall row remains a 0-pixel exact match. pass376 measurement-only overlays now cover 6 original-vs-Firestaff viewport rows, with large deltas still recorded as non-claims. Remaining work: promote debugger-observed same-state viewport rows for the nonzero gameplay crops and, separately, promote an I34E route transcript.
  - Data coverage: pass1057 locks the extracted DM1 Amiga 2.2 English `DUNGEONB.DAT` kid-dungeon sidecar (`9bac133b…`, 4,806 bytes) and makes the coverage row 3/3 registry-backed. This closes the DM1 variant-data acquisition gap; it does not affect PC34 runtime launch requirements.
  - 24h readiness roll-up: `tools/dm1_24h_readiness.py` now gives the active finish lane one PASS/FAIL command over DM1 data coverage, registry agreement, pass1056 viewport/wall pairing, pass1057 DUNGEONB lock, pass1058 keypad/route atlas, Phase A, playable-route, and pass1055 closed-door semantic CTests. Current status is PASS. This is a local readiness gate, not a claim that the remaining original-capture gaps are solved.
  - Wall: pass1052 now has a clean original DM1 PC 3.4 DOSBox turn-cycle capture with 2 `wall_closeup` frames, 0 duplicate raw hashes, and pass80 classifier PASS. pass1054 promotes one exact original-to-Firestaff 224x136 wall-crop match: original `02_left_1_wall_original_viewport_224x136.png` == Firestaff `hall_1_4_dirE_viewport_224x136.ppm`, 0 changed pixels / MAE 0. Remaining work: broaden to a deterministic multi-state wall route and promote more exact pairings.
  - Collision: pass1055 now has original DM1 PC 3.4 closed-door stasis evidence and a Firestaff-side semantic pair. Original `door_before`, `after_viewport_click`, and `after_kp5` are byte-identical at both raw-frame and 224x136 viewport-crop level; Firestaff replays the same movement sequence to `map=0 x=6 y=9 dir=3`, sees closed door square `0x94`, and blocks the next forward command without moving. Remaining work: pixel-pair this view and broaden to a full wall/door/fakewall collision transcript.
  - Creature-chain: pass1058 locks the corrected original DOSBox keypad mapping for the next route attempt (`F=kp8`, `B=kp2`, `TR=kp4`, `TL=kp6`; `kp5` also moves forward from the tested start pose). The corrected level-1 target route now reaches distinct original states, but the first selected target remains behind an inert closed door: enter, space, two clicks, and forward all leave the raw frame unchanged. Remaining work: choose a level-1 group with open line of sight, or use a controlled save/debug route, then promote a paired original creature screenshot. Creature render remains source-locked until that paired original capture exists.
  - Champion-panel: pass1053 now tracks original DM1 PC 3.4 candidate/resurrect-panel frames promoted from pass455 (`candidate_select` SHA256 `e4b373078be6aa0c27e793ccd476b6e886b34ef0c4b063c6d2274815351af53e`, terminal/HUD after C160 SHA256 `7523b67fa765ffb02a088bf8dbb0c2ba3630fcf5bcc2fb11f956b4e442b52b8f`). Firestaff V1 captures also exist (party_hud_four_champions_vga.ppm, party_hud_statusbox_gfx_vga.ppm). Remaining work: full four-champion party HUD + single-champion status-panel original pairing and Firestaff-vs-original pixel diffs.
  Canonical game data verified: DUNGEON.DAT SHA256 `d90b6b1c38fd17e41d63682f8afe5ca3341565b5f5ddae5545f0ce78754bdd85`, GRAPHICS.DAT SHA256 `2c3aa836925c64c09402bafb03c645932bd03c4f003ad9a86542383b078ecf8e`. Firestaff-side gates, source locks, and runtime routing are complete.
- 🔧 Inventory/chest polish beyond source-locked routes: DM1 portrait sensor parity is no longer a finish blocker: the 2026-06-21 pass1059 audit confirms C127 `sensorData` is the 0..23 C026 portrait-atlas ordinal, and the M11 runtime already rejects ordinals outside the mirror catalog. M12 launcher extras are also no longer a DM1 finish blocker: the 2026-06-21 pass1060 audit confirms the only disabled extras, spell reference and map viewer, are intentionally out-of-scope until there is a source-backed data source. Inventory pass1061 closes the stale `m11_obj_use()` consumable wiring gap, and pass1070 verifies the broader inventory route parity row across equip slots, backpack/chest, consumables, mouth/eye, panel-slot routing, object interaction, and M11 inventory runtime/pixel gates. Chest pass1062 promotes the existing pass652 scroll-wheel pickup-overflow runtime regression into CTest/Python verification, and the 2026-06-21 chest matrix now passes 38/38 across open/close, action-hand, full-hand, stack split/merge, scroll-wheel pickup, non-leader, party-rotate, candidate-panel, cross-champion, drop-to-floor, empty-reopen, and verifier gates. Champion-stat pass1063 closes the stale C01-C24 Hall-stat claim: runtime recruitment copies decoded mirror records through `F0606`/`F0652`/`F0673`, not flat helper defaults. C25/C26 pass1064 closes the stale Lord Order/Grey Lord projectile fallback gap with an explicit BUG0_13-safe Fireball route. Touch pass1065 closes the stale inventory/champion/menu zone rows against the existing source-locked touch matrix and mouse-command queue suite. Remaining chest-adjacent work is visual/pixel evidence only, tracked under B1 capture pairs and B3 V2 material gates.
- ✅ Creature AI finish scope: pass1066 closes the narrow DM1 pathfinding row with a data-free F0798/F0799 one-step cascade CTest; pass1067 adds data-free F0790-F0796 perception/target coverage; pass1069 closes the reaction/spell-behavior AI row with reaction-event creation, projectile-hit search-turn, danger movement/stop-attacking, and caster projectile payload/table gates. The 2026-06-21 grouping matrix also passes 8/8 across ordered attack cells, group move/removal, C006 unused group slot, source-locked creature behavior, pathfinding, perception/target, stairs/group timing, and pass803 ordered-cells verification. Broader creature-chain capture remains tracked as B1 capture evidence, not an AI-rule implementation gap.
- 🐛 P1 visual bugs needing capture/repro: (none — all four P1 visual bugs closed as of 2026-06-20). Treat each as unconfirmed until it has a reproducible capture or focused probe. **Blurry wall inscriptions FIXED (2026-06-20, BUG-DNY-DM1-2026-06-16):** root cause was the 5x7 fallback path in `m11_draw_dm1_front_wall_inscription_text` using `g_text_shadow` (shadowDx=1, shadowDy=1) which double-draws each glyph at (+1,+1) shadow + (0,0) main, producing 2px-thick overlapping glyphs that look blurry/double-exposed. Fix: use `g_text_small` (scale=1, tracking=1) with explicit `shadowDx=0`, `shadowDy=0`, `shadowColor=BLACK` so each glyph is drawn exactly once at its true 5x7 pixel position. Matches ReDMCSB DUNVIEW.C:3619-3638 (no shadow on M648_GRAPHIC_INSCRIPTION_FONT). Source: src/engine/m11_game_view.c:13618-13632. New probe `firestaff_m11_game_view_probe` INV_GV_407G smoke-tests the fallback path; the proper pixel-density heuristic needs a real DUNGEON.DAT with a front-wall inscription cell. **Champion mirrors not visible CLOSED (2026-06-20):** the new `firestaff_dm1_v1_champion_mirror_capture_probe` provides deterministic visual evidence — every positive-ordinal Hall pose shows the expected champion portrait (warm_count ≥ 30) and every negative-ordinal pose shows wall texture only (warm_count < 30). See DONE.md DM1 V1 2026-06-20 entry. **Champion Z-order/floating CLOSED (2026-06-20):** the same capture probe was extended with 6 no-floating side-wall scenarios — every (1,3)/(1,4) facing W/E/S pose shows wall texture only, no champion portrait floating over the side wall. See DONE.md DM1 V1 2026-06-20 entry. **Missing or incorrect viewport walls CLOSED (2026-06-20):** the new `firestaff_dm1_v1_viewport_wall_capture_probe` visits 6 Hall-of-Champions cells (DOOR/TELEPORTER/CORRIDOR/WALL) x 4 directions (24 poses total) and saves PPMs of every viewport, with grey-pixel + non-black + texture-diversity heuristics. Every pose shows substantial grey (23000-31000) + non-black (26000-30000) pixels confirming wall/floor/door geometry renders correctly. Visual inspection confirms correct wall placement, door frames, corridor geometry, and the MOPHUS/WUUF portrait alcoves on the appropriate side walls. See DONE.md DM1 V1 2026-06-20 entry.

### DM1 V2.0 / V2.1 / V2.2

- 🔧 Phase 3 - Modern UI overlay hardening: HUD/action route gates exist; remaining work is optional inventory, champion, rune, and action-panel polish without bypassing V1 command routes or inventory transactions.
- 🔧 Phase 4 - Lighting and visual effects hardening: palette/projectile metadata gates and field/projectile VFX binding gates exist; remaining work is full enhanced lighting, shadows, broader field effects, and deterministic fallback coverage.
- 🔧 Phase 5 - Smooth movement presentation hardening: runtime bridge/gates, optional Custom/V2 smooth turn-pan camera backend, pass1068 deterministic forward/back/left/right interpolation coverage, and full M11 `DM1_V2_CameraController` embedding are in place; remaining work is launcher UI polish while preserving V1 cooldowns, collision, sensors, creature timing, and redraw cadence.
- 🔧 Phase 6 - Touch/controller ergonomics hardening: route gates exist; remaining work is broader V2-only gesture/controller affordances with V1 touch/click parity preserved.
- 🔧 Phase 7 - V2 verification suite hardening: presentation-disabled state-hash and side-by-side seed pixel gates are now registered in CTest (`dm1_v2_source_route_state_hash_pc34`, `dm1_v2_side_by_side_seed_pc34`, `dm1_v2_v1_v2_side_by_side_seed_pc34`, `dm1_v2_side_by_side_presentation_seed_probe`). They cover deterministic source-route hashing, canonical V1/gap/V2 layout export, full-lane and D1C wall/portrait region parity, and N/E/S/W presentation-disabled viewport parity. Remaining work is real runtime screenshot-script artifacts for enhanced presentation modes.
- ✅ Phase 8 - V2.2 modern asset pipeline: presentation-mode selection, per-frame V22 shape cache, placeholder overlay fallback, modern asset discovery, and Apple-Silicon V20/V21/V22 renderer readbacks are wired and gated. 2026-06-21 adds cache-independent DM1 and CSB V2.2 in-place render probes (`dm1_v22_inplace_render_probe`, `csb_v22_inplace_render_probe`) that synthesize `v22_inplace_cache.bin`, verify inactive V1 fallback, bitmap lookup, 9-cell paint, and 4-direction sweeps. M11 live DM1 V2.2 now initializes the optional in-place cache and prefers it before falling back to the placeholder overlay. Remaining work: real PBR hero art beyond the first-cut pack plus per-cell `m11_draw_dm1_*`/CSB draw-pass swaps that consume real modern art in-place with material/pixel diffs.
- ✅ V2 presentation-mode selection wiring: dm1_v2_presentation_mode_pc34 + csb_v2_presentation_mode_pc34 modules (include/dm1_v2_presentation_mode_pc34.h, include/csb_v2_presentation_mode_pc34.h, src/dm1v2/dm1_v2_presentation_mode_pc34.c, src/csb/csb_v2_presentation_mode_pc34.c) expose M12_PRESENTATION_* selection with V22→V21→V20→V1 fallback chain. M11_GameView_Start in src/engine/m11_game_view.c calls dm1_v2_presentation_mode_set_m12() / csb_v2_presentation_mode_set_m12() right after the spec is built. CTest + headless probe coverage: 50+36 unit + 30+27 probe assertions across both modules (test_dm1_v2_presentation_mode_pc34 / test_csb_v2_presentation_mode_pc34 / firestaff_dm1_v2_presentation_mode_probe / firestaff_csb_v2_presentation_mode_probe), all pass alongside the 23/23 Phase A invariants.

## Chaos Strikes Back (CSB)

### CSB V1

- 🔧 Phase 2 - Dungeon data model: synthetic CSB dungeon loader/model probe exists and loader/free-cycle safety is covered; remaining work is real CSB asset ingestion and runtime structure parity without DM1-only assumptions.
- 🔧 Phase 3 - Rendering parity hardening: D3/D2 wall tables, bitmap selection, grid routing, CSB-only D3L2/D3R2 and D2L2/D2R2 draw-order/frame gates, F0107 back-wall ornament routing, and initial viewport gates exist; remaining work includes actual ornament blits, F0108 floor ornaments, F0115 creature/item/projectile pass, F0111 door panel, and `CustomBackgrounds`.
- ✅ GRAPHICS.DAT hidden-code item awareness (Atari ST 2.0/2.1 + Amiga 3.5/3.5 Multilanguage): skip-table and loader-integration gates now cover Atari ST CSB items 21/538/548 plus Atari/Amiga 558-562 and Amiga 21/676/686. Verification: `ctest --test-dir build -R 'csb_hidden_code_skip_table_unit|csb_v1_graphics_hidden_item_skip_pc34' --output-on-failure` PASS 2/2. Remaining CSB work is real-asset ingestion/rendering parity, not hidden-code skip awareness.
- ✅ Phase 6 - Utility/import flow: champion import from DM1 saves (256-byte CSB block format, DM1→CSB record conversion), import state machine (ReDMCSB SAVEGAME.C F0100-F0120), utility disk flow state machine (INIT→INSERT_DISK→VERIFY_DISK→DISK_OK→SELECT_ACTION→IMPORT/LOAD/NEW→DONE), headless probe passes 33/33 tests.
- ✅ Phase 7 - Verification suite: add deterministic boot, dungeon, combat, save/import, and rendering probes.
- 🐛 Runtime handoff: the M12 launch/profile intent is valid for hash-matched CSB assets; remaining work is title/intro/import path, CSB-specific viewport integration, and end-to-end playability verification.

### CSB V2.0 / V2.1 / V2.2

- ✅ Phase 0 - V1 compatibility lock before V2 work.
- ✅ Phase 1 - V2 launch/profile separation.
- 🔧 Phase 2 - Enhanced asset pipeline: presentation-mode selection API is wired (csb_v2_presentation_mode_set_m12, m12PresentationMode 0..3 → CSB_V2_PM_V1_FAITHFUL/V20_FILTERED/V21_UPSCALED/V22_MODERN). **2026-06-19 CSB V2.2 modern-asset module landed:** new `csb_v22_modern_assets_pc34.c/.h` (include + src) mirrors dm1_v2_modern_assets_pc34 with CSB-specific paths (`~/.firestaff/assets/csb/modern/`) and CSB source-locks (ReDMCSB LIGHT.C F0212 / DUNVIEW.C F0128 / PANEL.C F0354 / COMMAND.C:108-113,254-291 + CSBWin/Viewport.cpp:7290 + CSBWin/Chaos.cpp:60-69). Ctest `test_csb_v22_modern_assets_pc34` 33/33 (path resolution from dataDir, manifest validation missing/empty/partial, installed flag round-trip, epx warm flag round-trip, full fallback chain V1→V2.0→V2.1 cold/warm→V2.2 missing/installed, shape source name strings, missing placeholder 16x16 magenta, source evidence citation). **2026-06-19 CSB V2.2 first-cut asset pack landed:** `.openclaw/tmp/csb_v22_asset_author.py` procedural generator (5 PNGs: wall_dungeon/floor_prison/creature_chaos_fiend/panel_lord_order/champion_warrior_csb + modern_asset_manifest.json v1.0.0). CSB-specific palette accents: CHAOS_PURPLE (chaos magic), IRON_GREY (prison), LORD_GOLD (Lord Order). Smoke: `csb_v22_set_manifest_path(dataDir)` resolves correctly, `csb_v22_modern_assets_available()=1` end-to-end against real CSB data dir. **2026-06-21 CSB in-place render gate:** `csb_v22_shape_cache_update` now respects `csb_v2_presentation_mode_is_v22()`, `csb_v22_inplace_draw_init()` loads from `~/.firestaff/assets/csb/modern/`, and `csb_v22_inplace_render_pass()` uses the cache's 0..2 depth coordinates. New CTest `csb_v22_inplace_render_probe` PASS verifies a synthetic cache, V1 inactive cache, V22 bitmap lookup, 9 painted CSB cells, 4-direction sweep (36 cells), and source evidence. Remaining work: real PBR hero art (openai/gpt-image-2) for CSB + per-cell modern-art swap in CSB's 9-square viewport draw passes + per-mode pixel/material verification gates.
- 🔧 Phase 3 - Enhanced UI overlays: scaffolded (HUD compass/depth/gold/champion bars/action strip/chaos indicator, csb_v2_hud_overlay_pc34.h/.c, build+probe pass). Mode selection gate added in this pass (csb_v2_presentation_mode_is_v22() / is_v21() / is_v20() / is_v1()) so the HUD overlay can branch on the active mode.
- ✅ Phase 4 - Enhanced lighting and magic effects (probe landed 2026-06-17): new `firestaff_csb_v2_phase4_enhanced_lighting_probe.c` covers lighting brightness boost + color-temperature shift per-zone (CSB's magic zones: neutral/poison/lord-chaos/anti-magic) + magic effect bloom. Source-locked against Chaos Strikes Back chaos magic lighting (csb_v1_chaos_magic_pc34_compat.c) + ReDMCSB LIGHT.C F0212 + ReDMCSB DUNVIEW.C F0128 + ReDMCSB PANEL.C F0354.
- ✅ Phase 5 - Smooth movement and viewport interpolation (2026-06-17): `csb_v2_smooth_movement.c` (50/50) + `csb_v2_runtime.c` binding seam (12 test groups, 43/43) on top of F0365/F0366/F0364 + new `csb_v2_smooth_movement_runtime.c/h` providing the V1→V2 smooth-movement bridge layer (mirrors dm2_v2_smooth_movement_runtime / nexus_v2_smooth_movement_runtime pattern). Phase-gated tick wrapper: per-frame tick is a no-op when V1 is active presentation. Source-locked against ReDMCSB GROUP.C:1695-1770 (F0207), skproject/SKWIN/SkWinCore.cpp (ease-out cubic), CSBWin/resurrect/CsbV2FieldSmooth.cpp (CSBWin reimpl), ReDMCSB GAMELOOP.C:47-50 (V1 tick cadence 55ms). Remaining work: pixel/presentation gates for full deterministic input coverage.
- ✅ Phase 6 - Touch/controller ergonomics (probe landed 2026-06-17): new `csb_v2_touch_runtime.c/h` provides the V1→V2 input bridge layer (mirrors dm2_v2_touch_runtime / nexus_v2_touch_runtime pattern). Translates CSB V2 touch/controller affordances (TOUCH_SWIPE_*, EDGE_STRAFE_*, CONTROLLER_DPAD_*, LEFT/RIGHT_STICK_*) into V1 command-queue entries. CSB's affordance router already returns DM1_V2_MovementCommand values directly so no NEXUS_CMD_* translation step is needed (unlike Nexus V2). Phase-gated: V2 launch+profile both required (or `force_active_for_test(1)`). Source-locked against ReDMCSB COMMAND.C:108-113/254-291, CLIKMENU.C:142/180, GAMELOOP.C:164-219. Probe `firestaff_csb_v2_touch_runtime_probe` 88/88 assertions.
- ✅ Phase 7 - V2 verification suite (2026-06-17, hardened 2026-06-17): `firestaff_csb_v2_phase7_verification_probe` 120/120 (was 82/82, +38 new pixel gates). Original coverage: presentation-disabled state-hash gate + deterministic input scripts + side-by-side V1/V2 state equality + phase gate domain coverage + source evidence. **Pixel gates added this pass:** (1) **V1 framebuffer byte-preservation (V2 disabled)** — sentinel 320x200 indexed framebuffer loaded with unique deterministic pattern (byte[k] = k*7+13), then 4 V2 runtime modules (chaos_enhanced, vfx_particles, touch_runtime, lighting_dynamic) invoked with V2 disabled and the framebuffer byte-compared to the sentinel. Any byte difference means V2 is leaking into V1 chrome. (2) **V1 framebuffer byte-preservation (V2 enabled)** — same sentinel pattern, lighting_dynamic forced active, framebuffer must still be preserved (V2 has its own presentation framebuffer, never writes to V1's). (3) **Viewport render byte-determinism** — two CSB_V2_ViewportState instances given identical tick_schedule must produce identical observable state (scale_factor, epx_enabled, custom_bg_active, prison_door_progress, clock.dt_ms). Sub-tick float must also be IEEE 754 bit-identical. (4) **State-hash equality across V2 modes** — V2_OFF, V2_UPSCALED, V2_ENHANCED must all produce identical gameplay state-hash (only render output differs). Source-locked against CSBWin/Viewport.cpp:1-180 (CSB indexed framebuffer) + ReDMCSB DUNVIEW.C:1-50 + PANEL.C:418-428. CMakeLists: csb_v2_vfx_particles.c + csb_v2_lighting_dynamic.c + csb_v2_touch_runtime.c + csb_v2_smooth_movement_runtime.c + fs_portable_compat.c added to probe sources; firestaff_v2 lib added to link libraries. `csb_v2_chaos_fire_projectile()` stub added (delegates to csb_v2_vfx_fire_projectile).

## Dungeon Master II: Skullkeep (DM2)

### DM2 V1

- ✅ Phase 2 - Dungeon/world data model: complete DM2 map, object, tile, and world-state ingestion.
- ✅ Phase 3 - Rendering pipeline: viewport, UI chrome, items, outdoor/indoor, palette behavior (pass).
- 🔧 Phase 4 - Mechanics parity: movement, interactions, shops/NPCs, doors, pressure plates, triggers, combat, magic, and timeline. **2026-06-16 combat parity landed:** `dm2_v1_combat.c` expanded from 30-line stub to full Phase 5 source-locked resolver. CTEST 49/49 + probe 13/13. **2026-06-17 spell + tech/magic parity landed:** `dm2_v1_spell.c` expanded with Phase 4 spell casting mechanics — `validate_runes()`, `mana_cost()`, `compute_chance()`, `can_cast()`, `cast_attempt()` returning full `DM2_SpellCastResult { success, mana_used, cooldown_ticks, skill_decay, effective_difficulty, effective_chance }`. Spell table updated with realistic `mana_per_rune` values (POTION=2-3, MISSILE=4-6, SUMMON=8, GENERAL=1-4) so the per-rune mana deduction contract matches DM2's SKULL.ASM behavior. `dm2_v1_tech_magic.c` expanded with `lookup()` (known-item catalog: crossbow/pistol/rifle/bomb/lantern/magic_battery/flame_orb/potions), `consume_charge()` (decrements available charges, supports unlimited -1), `hybrid_power()` (tech*25 + magic*25 capped at 100, returns 0 for non-hybrid items). Source-locked against skproject/SKWIN/SkWinCore.cpp:17521-17670 (CAST_SPELL_PLAYER), 18159-18174 (ADD_RUNE_TO_TAIL), SkGlobal.cpp:966-1011 (dSpellsTable), SkGlobal.h:37-55 (MAXSPELL_ORIGINAL=34), SkWinCore.cpp:27038-27096 (spell→OBJECT_EFFECT), SKULL.ASM tech/magic item routines. CTEST `test_dm2_v1_spell_pc34_compat` 38/38 (spell table integrity, rune validation, mana cost formula, cast chance formula, cast attempt success/fail with cooldown + skill_decay, can_cast pre-check, OBJECT_EFFECT resolution, DM2-only spells, source evidence; tech/magic lookup, can_use respects tech/magic levels, hybrid requires BOTH, power cost manual/battery/mana/hybrid/empty, charge consumption including unlimited, hybrid power formula + cap). Remaining Phase 4 work: shops/NPCs, pressure plates, triggers, timeline wiring.
- 🔧 Phase 5 - Creature/combat parity: creature AI table (64 entries with names + AI flags, 352-line implementation in `dm2_v1_creature.c` with spawn/tick/death_check) + combat resolver (now Phase 5-locked above) are source-locked. **2026-06-17 projectile routing + death sound landed:** new `dm2_v1_projectile_pc34_compat.c/h` provides the DM2→DM1 projectile bridge — maps DM2 creature `AttacksSpells` flags (12 bits: SHOOT/FIREBALL/LIGHTNING/DISPELL/POISON_CLOUD/POISON_BOLT/POISON_BLOB/PUSH_BACK) to DM1 `PROJECTILE_CATEGORY_*` + `PROJECTILE_SUBTYPE_*` via `dm2_v1_projectile_pick_category()`, then dispatches via F0810_PROJECTILE_Create_Compat. Three dispatch entry points: `dm2_v1_projectile_dispatch()` (auto-pick from creature AI flags), `dm2_v1_projectile_dispatch_spell()` (CCM 0x15 CAST_SPELL explicit subtype), `dm2_v1_projectile_dispatch_bomb()` (DM2 new area-effect). Plus magic-number fix in `dm2_v1_creature.c`: creature death sound now uses `DM2_SOUND_CREATURE_DEATH` constant instead of hardcoded `0x11`. New accessor `dm2_v1_creature_get_instance()` exposes creature pool read-only to the projectile module. Source-locked against SKULL.ASM:10620-10710 (SKULL_COMBAT_ResolveRanged), 11100-11200 (projectile routing), ReDMCSB PROJEXPL.C:76-92 (F0212), GROUP.C:1695-1770 (F0207 creature attack), skproject/SKWIN/SkWinCore.cpp:10479-10561 (AI_W30_TURNS_MISSILE). CTEST `test_dm2_v1_projectile_pc34_compat` 23/23 (all 7 attack-flag → category mappings, dispatch invalid/dead/melee-only rejection, archer guard + amplifier dispatch, spell + bomb dispatch, 3 observability counters, reset, source evidence, magic-number constant check). Remaining work: advanced CCM (DM2_PROCEED_CCM) full implementation, projectile-list drain back into M11 renderer.

### DM2 V2.0 / V2.1 / V2.2

- ✅ Phase 0 - V1 compatibility lock before V2 work.
- ✅ Phase 1 - V2 launch/profile separation: DM2_V2_PHASE_DOMAIN_LAUNCH and _PROFILE gates implemented; 42/42 probe pass; commit 22838e8f.
- 🔧 Phase 2 - Enhanced asset pipeline: `dm2_v2_asset_pipeline.c` (V2.1 EPX + V2.2 modern-asset fallback chain) is source-locked against SKULL.ASM T520/T560/T580/T600 + ReDMCSB DUNVIEW.C:575-586/148-157/2962-3047/3048-3070/3082-3095/3940-4015/4016-4050/4119-4270 + PANEL.C:418-428 + DATA.C:359-360; probe `firestaff_dm2_v2_phase2_asset_pipeline_probe` is green. **2026-06-19 DM2 V2.2 modern-asset module landed:** new `dm2_v22_modern_assets_pc34.c/.h` mirrors dm1/csb/theron/nexus modules with DM2 paths (`~/.firestaff/assets/dm2/modern/`) and DM2 source-locks (SKULL.ASM T520/T560/T600 + ReDMCSB DUNVIEW.C:2962-3047 outdoor). Ctest `test_dm2_v22_modern_assets_pc34` 33/33. **2026-06-19 DM2 V2.2 first-cut asset pack landed:** `.openclaw/tmp/dm2_v22_asset_author.py` (5 PNGs + manifest v1.0.0). Smoke: `dm2_v22_modern_assets_available()=1` end-to-end. Remaining: real PBR hero art for DM2 via gpt-image-2 batch + DM1 V2.2 in-place drawing pipeline + per-cell modern-art swap in DM2 V1 draw pipeline (indoor T560 + outdoor T600).
- 🔧 Phase 3 - Enhanced UI overlays: **2026-06-16 HUD runtime wire-up landed (this pass):** new `dm2_v2_hud_runtime.c/h` provides the V1→V2 HUD bridge layer (mirrors `csb_v2_hud_runtime.c`). API: init/shutdown, set_gate_config, set_party_gold, set_direction, set_level, set_champion, set_action_active, trigger_hit_flash, set_opacity, render (gated on DM2_V2_PHASE_DOMAIN_HUD, V1 framebuffer preserved when V1 active), is_active, force_active_for_test. Source-locked against SKULL.ASM T560, skproject/SKULLWIN/c_gui_vp.cpp, ReDMCSB PANEL.C F0354, DUNGEON.C F0260, COMMAND.C, DISPLAY.C, dm2_v2_phase_gate.h. **M11 wire-up:** `firestaff_game_loop.c` (src/engine) now calls `dm2_v2_hud_runtime_render(g_framebuffer, 320, 200)` right after the DM2 V2 smooth-movement viewport render, gated on phase gate (no-op when V1 active, no V1 chrome pollution). CMakeLists: `firestaff_dm2_v2` linked from `firestaff_m11`. Probe `firestaff_dm2_v2_hud_runtime_probe` 23/23 (init/shutdown, all 7 setters, gated render is no-op when V2 off, paints into fb when V2 on, opacity=0 short-circuits, force_active_for_test bypass, V1 framebuffer preserved, champion bar pixels, action strip pixels, null-fb safe, source evidence). Remaining work: actual HUD text/bitmap assets, more HUD widgets (inventory quick-view, action prompt).
- ✅ Phase 4 - Enhanced lighting and outdoor effects (M11 wire-up landed 2026-06-17): new `dm2_v2_lighting_runtime.c/h` provides the V1→V2 lighting + outdoor FX bridge layer (mirrors csb_v2_hud_runtime / dm2_v2_touch_runtime pattern). API: init/shutdown, set_gate_config, tick(dt_seconds, weather) — phase-gated on DM2_V2_PHASE_DOMAIN_HUD (shared gate with HUD), is_active, get_state/get_outdoor_fx (read-only accessors that return NULL when V2 disabled), force_active_for_test. **M11 wire-up:** `firestaff_game_loop.c` calls `dm2_v2_lighting_runtime_init()` right after `dm2_v2_touch_runtime_init()` in the DM2 V1 boot path, then calls `dm2_v2_lighting_runtime_tick(0.055f, 0)` per render frame after the HUD render — advances lighting.bloom_timer + outdoor FX state with the V1 tick cadence (55ms). Phase-gated: no-op when V1 is active (lighting state preserved for V1 palette). Source-locked against SKULL.ASM PROCESS_TIMER_0C (per-champion torch timers), SKULL.ASM T560 (indoor dungeon viewport), SKULL.ASM T600 (outdoor viewport), ReDMCSB PANEL.C:367-428 (DM1 palette lighting semantics), dm2_v2_lighting.c, dm2_v2_outdoor_enhanced.c. Headless probe `firestaff_dm2_v2_lighting_runtime_probe` 38/38 (init/shutdown lifecycle, no-op when V2 disabled, tick advances state when V2 enabled, multiple weather values, force_active_for_test bypass, observability counter, V1 invariant state preservation, read-only accessors return NULL when inactive, source evidence citation).
- ✅ Phase 5 - Smooth movement and viewport interpolation (M11 V1 game-loop wire-up landed 2026-06-17): `dm2_v2_runtime_v1_tick(now_ms)` already wired in `firestaff_game_loop.c` V1 game-loop entry point (advances V2 animation clock, torch flicker, bloom fade, smooth movement observer). **2026-06-17 extension:** added `dm2_v2_lighting_runtime_tick(0.055f, 0)` call to V1 game-loop entry point so outdoor FX state (cloud drift, ambient tint, lightning sequence) advances per V1 tick cadence. New `firestaff_dm2_v2_m11_game_loop_wireup_probe` 36/36 (verifies full V2 runtime chain from M11 V1 game-loop entry point: HUD + touch + lighting all coexist, phase gate drives all in lockstep, lighting tick advances over multiple V1 ticks with different weather values, touch translation produces correct DM1_V1_COMMAND_* entries for 4 affordance types, V1 framebuffer preservation union). Source-locked against SKULL.ASM T520 (party/movement tick), T560 (dungeon viewport), T600 (outdoor viewport), ReDMCSB GAMELOOP.C:47-50 (V1 tick cadence 55ms), 164-219 (V1 input/cadence). Local ctest 23/23 DM2 tests pass (1 new probe added).
- ✅ Phase 6 - Touch/controller ergonomics (M11 wire-up landed 2026-06-17): new `dm2_v2_touch_runtime.c/h` provides the V1→V2 input bridge layer that translates touch/controller affordances (`DM2_V2_AFFORDANCE_TOUCH_SWIPE_*`, `EDGE_STRAFE_*`, `CONTROLLER_DPAD_*`, `LEFT_STICK_*`, `RIGHT_STICK_*`) into V1 command-queue entries (`Dm1V1QueuedCommandPc34Compat`) via the existing `dm2_v2_touch_controller_affordance_route()` + `dm1_v2_movement_command_route_for_presentation()`. **M11 wire-up:** `firestaff_game_loop.c` calls `dm2_v2_touch_runtime_init()` right after `dm2_v2_hud_runtime_init()` in the DM2 V1 boot path, so the runtime is ready to translate SDL touch/controller events. Phase-gated: V2 launch+profile both required (or `force_active_for_test(1)`); V1 parity guard preserves the V1 mouse/touch/click route matrix as the sole input path when V1 is the active presentation. Source-locked against SKULL.ASM T520/T048/T560, ReDMCSB COMMAND.C:108-113/254-291, ReDMCSB CLIKMENU.C:142/180, ReDMCSB GAMELOOP.C:164-219. Headless probe `firestaff_dm2_v2_touch_runtime_probe` 50/50 assertions (init/shutdown lifecycle, 12 affordance→DM1_V1_COMMAND_* translations covering swipe + edge-strafe + dpad, phase-gate rejection when V2 off, V2 partial rejection, NULL out-pointer safety, force_active_for_test bypass, observability counter increments only on accepted translations, source evidence citation, post-shutdown rejection, coordinate pass-through, NONE-affordance rejection).
- ✅ Phase 7 - V2 verification suite (2026-06-17): new `firestaff_dm2_v2_verification_suite_probe.c` provides end-to-end verification that all 6 DM2 V2 phases share the same phase gate, coexist correctly, and never pollute the V1 framebuffer when V2 is disabled. Coverage (44 assertions across 8 groups):
  * **Phase 0 — V1 compatibility lock:** defaults to V1-only, all 4 phase domains return `v1SourceLocked=1` by default
  * **Phase 1 — Launch/Profile separation:** LAUNCH and PROFILE are independent toggles; PROFILE requires both flags
  * **Phase 2 — Asset pipeline gate:** ASSET_PIPELINE domain gates correctly on V2 enabled/disabled
  * **Phase 3 — HUD runtime wire-up:** HUD render paints pixels when V2 enabled + visible, V1 framebuffer preserved byte-for-byte when V2 disabled, force_active_for_test bypasses gate
  * **Phase 4 — Lighting + outdoor FX:** `dm2_v2_lighting_init` initializes state, `dm2_v2_lighting_tick_bloom` advances bloom timer, `dm2_v2_outdoor_fx_init/tick` advances cloud drift + ambient tint, `dm2_v2_outdoor_fx_trigger_lightning` sets flash flag
  * **Phase 5 — Smooth movement:** `dm2_v2_smooth_init` starts idle, walk/turn/stairs activate animations, `dm2_v2_smooth_tick` advances
  * **Phase 6 — Touch/Controller runtime:** translation rejected when V2 disabled, accepted when V2 enabled, force_active_for_test bypass, observability counter
  * **Cross-phase invariants:** all 4 V2 runtime modules init independently, phase gate config drives all runtimes consistently (turning V2 off disables all in lockstep), V1 framebuffer preservation union — when V2 disabled, combined HUD + touch runtimes must not modify any byte of a pre-loaded V1 sentinel framebuffer

Source-locked against SKULL.ASM T520/T560/T600, ReDMCSB GAMELOOP.C:164-219, and all 6 V2 phase module headers. Headless probe 44/44 assertions.

### DM2 CLI launch

- ✅ `--game dm2` direct-launch wired (closed 2026-06-17 by adding M11_GameView_StartDm2 branch + fixing dm2_md5_body in src/dm2/dm2_v1_boot.c). verify_all_launches.sh now 59/59 PASS (was 49/49 + 10 FAIL_NODATA). See DONE.md for full fix details.

## Dungeon Master Nexus

### Nexus V1

- 🔧 Runtime handoff/playability proof: V1 phases 0-7 are implemented/source-locked; remaining work is launcher/game-loop handoff and real Saturn asset-path proof.
- 🔧 Mechanics parity hardening: movement, click routes, item usage, doors, pits, teleporters, triggers, combat, AI, and sound are implemented; remaining work is broader runtime/probe coverage beyond compile/save-load gates.

### Nexus V2.0 / V2.1 / V2.2

- ✅ Phase 0 - V1 compatibility lock: `nexus_v2_phase_gate_pc34.c` (include/nexus_v2_phase_gate_pc34.h) classifies 19 V1/V2 domains (11 V1-source-locked gameplay + 9 V2-presentation-eligible). V1-locked domains (DMDF_DGN_LOADING, SATURN_ISO_READER, GAME_STATE_INIT, CHAMPION_PARTY, CREATURE_AI, SPELL_MAGIC, MOVEMENT, SAVE_LOAD, SOUND_DRIVER, RASTERIZER, INVENTORY) always return v1SourceLocked=1 and v2PresentationAllowed=0 regardless of V2 toggles. V2-eligible domains (RENDER_PRESENTATION, SMOOTH_MOVEMENT_PRESENTATION, DYNAMIC_LIGHTING_PRESENTATION, HUD_OVERLAY, PARTICLE_EFFECTS, ATMOSPHERE, INPUT_PRESENTATION, CONFIG_PRESENTATION, UPSCALER) require v2PresentationEnabled=1; CONFIG_PRESENTATION additionally requires v2ConfigPersistenceEnabled=1. Source-locked against NEXUS.C / NEXUS2.C / NEXUS.BIN, nexus_v1_iso_reader.c, nexus_v1_dmdf_model.c, nexus_v1_dungeon.c, nexus_v1_engine.c, nexus_v1_game.c, nexus_v1_champions.c, nexus_v1_creatures.c, nexus_v1_movement.c, nexus_v1_combat.c, nexus_v1_magic.c, nexus_v1_inventory.c, nexus_v1_save_load.c, nexus_v1_sound.c, nexus_v1_rasterizer.c, ReDMCSB CLIKMENU.C:142/180 F0365/F0366, COMMAND.C:2045 F0380, MOVESENS.C:316-345 F0267, HuC6260/HuC6270 VDC/VCE, THQUEST.ASM T400-T900. Ctest `nexus_v2_phase_gate_pc34` 240/240, headless probe `firestaff_nexus_v2_phase0_v1_compatibility_lock_probe` 218/218.
- ✅ Phase 1 - V2 launch/profile separation: headless probe `firestaff_nexus_v2_phase1_launch_profile_separation_probe` 60/60. Verifies launch gate (GAME_STATE_INIT is V1-source-locked, always allowed; RENDER_PRESENTATION blocked when V2 off, allowed when V2 on), profile gate (CONFIG_PRESENTATION requires BOTH v2PresentationEnabled=1 AND v2ConfigPersistenceEnabled=1, with V2 off/persist on still blocked by V1 gate), Nexus V1 asset hash separation (0DMSTRT.BIN 8a026f1...d20b6, DM.BIN 3bbca12...ad124, LEV00.DGN 24e3b3c...83d9a, LEV15.DGN df8ccdf...ef0aa, ITEM.IBS fc32ca5...30c1, FACE.BIN d733f50...21e22, FILE_LISTING.txt 6526c88...9091b), cross-game hash separation (Nexus LEV00.DGN ≠ DM1 DUNGEON.DAT d90b6b1c38fd17e41d63682f8afe5ca3341565b5f5ddae5545f0ce78754bdd85 ≠ CSB DUNGEON.DAT 6695d2acebce49f95db1d8f3a5c733de), V1-only default behaviour, headless-safety (no game data files loaded). Ctest `nexus_v2_phase1_launch_profile_separation` 1/1.
- 🔧 Phase 2 - Enhanced asset pipeline: presentation-mode selection API + filter config + V2.1 EPX upscaler pipeline are wired (`nexus_v2_upscaler.c` provides `nexus_v2_epx_upscale` indexed→RGBA via palette, `nexus_v2_bilinear_smooth` post-filter, `nexus_v2_upscaler_source_evidence`). Headless probe `firestaff_nexus_v2_upscaler_probe` 23/23 (palette lookup, deterministic output for same input, 2x scaling fills all dst pixels, 1x1 boundary case, null-arg safety on src/dst/palette/zero-dims, bilinear null-arg safety on null/0x0/1x1/4x4, source evidence). Ctest `nexus_v2_upscaler_probe` 1/1. **2026-06-19 Nexus V2.2 modern-asset module landed:** new `nexus_v22_modern_assets_pc34.c/.h` mirrors dm1/csb modules with Nexus paths (`~/.firestaff/assets/nexus/modern/`) and Saturn source-locks (SATURN_DMDF T400/T520/T600 + Saturn VDP1/VDP2). Ctest `test_nexus_v22_modern_assets_pc34` 33/33. **2026-06-19 Nexus V2.2 first-cut asset pack landed:** `.openclaw/tmp/nexus_v22_asset_author.py` (5 PNGs + manifest v1.0.0). Smoke: `nexus_v22_modern_assets_available()=1` end-to-end. Remaining work: real PBR hero art for Nexus via gpt-image-2 batch + per-cell modern-art swap in Nexus V1 draw pipeline.
- ✅ Phase 3 - Enhanced UI overlays: `nexus_v2_hud_overlay.c` (include/nexus_v2_hud_overlay.h) provides presentation-only HUD chrome with compass rose (4-way N/E/S/W), dungeon depth counter (cur/max), gold piece counter, 4-champion mini-bars (HP/St/Ma/leader/spell_ready), action strip (ATTACK/CAST/USE/DROP/MOVE), Saturn panel indicators (menu/map/party), hit-flash feedback, low-health pulse, and smooth compass needle rotation. `Nexus_V2_HudOverlay` is the master state struct. API: `nexus_v2_hud_init/reset`, `set_direction/set_level/set_gold/set_champion_bar/set_action_active`, `trigger_hit_flash`, `toggle`, `set_opacity`, `render`, `source_evidence`. Default: visible=1, opacity=255, depth 1/10, all icons inactive. set_level clamps max<=0 to 1, set_direction clamps to 0..3. Headless probe `firestaff_nexus_v2_hud_overlay_probe` 65/65 (init defaults, reset returns to defaults, all setters store state, hit_flash + toggle + set_opacity, render deterministic for same state, invisible HUD leaves buffer unchanged, null-arg safety across all 11 entry points, source evidence NEXUS.BIN/VDP1/VDP2/DMDF/PANEL.C/DUNGEON.C). Ctest `nexus_v2_hud_overlay` 1/1. Source-locked against Saturn NEXUS.BIN HUD surface data, DMDF parser documentation, Saturn SDK VDP1 bitmap surfaces + VDP2 background layers, ReDMCSB PANEL.C (champion status refresh), DUNGEON.C (party stat bar refresh timing).
- ✅ Phase 4 - Enhanced lighting and Saturn presentation effects: `nexus_v2_lighting.c` (include/nexus_v2_lighting.h) provides per-vertex dynamic lighting with torch held by party (warm radius + flicker), spell cast flash, dungeon-level ambient, and distance attenuation. `Nexus_V2_LightingState` holds up to NEXUS_MAX_LIGHTS=16 lights, ambient RGB, and `torch_flicker_phase`. `nexus_v2_lighting_init()` zeroes state + sets ambient defaults (0.15/0.12/0.10). `nexus_v2_light_add()` inserts lights (returns slot index or -1 on full); `nexus_v2_light_remove()` clears active. `nexus_v2_lighting_tick(dt)` advances `torch_flicker_phase` (dt*8.0) and recomputes flickering light intensity into [radius*0.5*0.85, radius*0.5*1.0] range. `nexus_v2_apply_lighting(rgba, w, h, ls, cam_x, cam_y, cam_z)` applies ambient + per-light contribution to the RGBA framebuffer in-place (deterministic for same inputs). Source-locked against Saturn NEXUS.BIN (VDP1 polygon lighting / VDP2 shadow layer), DMDF level data (per-tile light emission values, DGN format), ReDMCSB LIGHT.C F0380 (light radius + flicker timing), COMMAND.C F0209 (spell-light colour binding), DUNGEON.C (torch position tracking in party state). Ctest `nexus_v2_lighting` 79/79, headless probe `firestaff_nexus_v2_lighting_probe` 59/59.
- ✅ Phase 5 - Smooth movement and viewport interpolation (M11 wire-up landed 2026-06-17): new `nexus_v2_smooth_movement_runtime.c/h` provides the V1→V2 smooth-movement bridge layer. Mirrors dm2_v2_touch_runtime / dm2_v2_lighting_runtime pattern with phase gate (`nexus_v2_phase_gate_pc34`), init/shutdown, tick(dt_seconds), and force_active_for_test bypass. Translates V1 walk/turn/stairs deltas into per-frame smooth interpolation deltas consumed by the V2 render pipeline. **`firestaff_game_loop.c`** wires `nexus_v2_smooth_movement_runtime_init()` + `_tick(0.055f)` for gameId=nexus in V1 boot path. Probe `firestaff_nexus_v2_smooth_movement_runtime_probe` covers init/shutdown lifecycle, no-op when V2 disabled, tick advances interpolation state when V2 enabled, walk/turn/stairs delta handling, force_active_for_test bypass, observability counter, V1 invariant state preservation. Source-locked against Saturn NEXUS.BIN VDP1 transformation tables, NEXUS.C interpolation parameters, ReDMCSB DUNVIEW.C F0128 viewport interpolation. Remaining work: broader deterministic input coverage and pixel/presentation gates.
- ✅ Phase 6 - Touch/controller ergonomics (M11 wire-up landed 2026-06-17): new `nexus_v2_touch_runtime.c/h` provides the V1→V2 input bridge layer. Translates Nexus V2 touch/controller affordances (`NEXUS_V2_AFFORDANCE_TOUCH_SWIPE_*`, `EDGE_STRAFE_*`, `CONTROLLER_DPAD_*`, `LEFT_STICK_*`, `RIGHT_STICK_*`) into V1 command-queue entries (`Dm1V1QueuedCommandPc34Compat`) via the existing `nexus_v2_touch_controller_affordance_route()` + DM1 V2 movement command adapter. **Translation chain:** `nexus_v2_touch_controller_affordance_route` returns NEXUS_CMD_* (1-6) values; the runtime translates NEXUS_CMD_FORWARD/BACKWARD/TURN_LEFT/TURN_RIGHT/STRAFE_LEFT/RIGHT to the corresponding DM1_V2_MOVEMENT_COMMAND_* (3/5/1/2/6/4) values before the V1 command dispatch. Phase-gated: V2 launch+profile both required (or `force_active_for_test(1)`); V1 parity guard preserves the V1 mouse/touch/click route matrix as the sole input path when V1 is the active presentation. Source-locked against Saturn NEXUS.BIN touch/joypad, Saturn SDK JOYPAD API, ReDMCSB COMMAND.C:108-113/254-291, CLIKMENU.C:142/180, GAMELOOP.C:164-219. Probe `firestaff_nexus_v2_touch_runtime_probe` 57/57 assertions (init/shutdown lifecycle, 6 affordance→DM1_V1_COMMAND_* translations covering swipe + edge-strafe + dpad, phase-gate rejection when V2 off, V2 partial rejection, NULL out-pointer safety, force_active_for_test bypass, observability counter increments only on accepted translations, source evidence citation, post-shutdown rejection, coordinate pass-through, NONE-affordance rejection).
- ✅ Phase 6 - Touch/controller ergonomics: V2-only touch swipe, edge-strafe, D-pad, and dual analog stick affordances for the Saturn gamepad mapped to `NEXUS_CMD_FORWARD/BACKWARD/TURN_LEFT/TURN_RIGHT/STRAFE_LEFT/RIGHT` (1-6), with V1 mouse/touch/click parity preserved (affordances rejected when `v2PresentationEnabled=0`). Ctest target `test_nexus_v2_touch_controller_affordance` covers mapping/classification/routing/name/source-evidence invariants; headless probe `firestaff_nexus_v2_touch_controller_affordance_probe` passes 294/294 (API surface, movement command mapping, input kind classification, V2 presentation route, V1 parity guard, NONE handling, idempotency, Saturn-specific right-stick-turn-only, cross-game shape consistency with DM1/DM2/CSB sibling affordances, source evidence, route kind constants).
- ✅ Phase 7 - V2 verification suite: headless probe `firestaff_nexus_v2_verification_suite_probe` 35/35 (V2 OFF init + byte-stability output[k] = palette[color_buffer[k]] + determinism, V2 UPSCALED init (640x400) + 2x scaling, V2 ENHANCED init (1280x800) with all V2.2 features ON, state-hash gate (deterministic for same V1 input), config->dim mapping for all 3 modes, lifecycle re-init after shutdown, null-arg safety on all 4 pipeline entry points). Ctest `nexus_v2_verification_suite_probe` 1/1. Source-locked against NEXUS.C / NEXUS2.C / NEXUS.BIN, DMDF level data, ReDMCSB DUNVIEW.C F0128. Disjoint from existing Nexus V2 lanes (lighting probe, HUD overlay probe, upscaler probe, smooth movement, touch, particles, atmosphere). Remaining work: full side-by-side V1/V2 deterministic input scripts plus screenshot/pixel gates for enhanced presentation.

## Theron's Quest

### Theron V1

- ✅ Runtime handoff/playability proof: hash-verified Track 02 availability is wired through validator/startup/menu state, JP canonical + JP extras + US extras now launch to the TQR level-load milestone through `tier1_strict_boot_probe`, and the focused reuse/bank gates (`theron_v1_launcher_scan_reuse`, `theron_v1_track02_bank`) are green. Remaining Theron V1 work is no longer positive launch proof; it is cross-slot import/export against real saves and broader gameplay-route evidence.
- 🔧 Phase 5 - Mechanics parity hardening: 50-assertion mechanics probe covers movement, click routes, doors, pits, teleporters, altar, combat, drops, and sounds; remaining work is real-asset gameplay traces and broader cross-route runtime evidence.
- 🔧 Phase 7 - Save/import compatibility: round-trip, header-rejection, world-serialize-purchase-state, and shop price-table regressions are green; remaining work is cross-slot import/export against real Track 02 saves.
- ✅ Phase 8 - Verification suite: launch, dungeon progression, mechanics, rendering, save/load, direct-launch, M11-handoff, launcher scan-reuse, Track 02 bank, and Tier 1 strict real-asset launch gates are green for Theron JP canonical, JP extras, and US extras. Remaining verification work is cross-slot save evidence and broader real-route gameplay traces.

### Theron V2.0 / V2.1 / V2.2

- ✅ Phase 0 - V1 compatibility lock: `theron_v2_phase_gate_pc34.c` (include/theron_v2_phase_gate_pc34.h) classifies 16 domains (12 V1-source-locked + 4 V2-presentation-eligible). V1-locked domains (TRACK02_BANK, BOOT_PROFILE, CHAMPION_PARTY, DUNGEON_PROGRESSION, MECHANICS, SAVE_LOAD, SHOP, TILE_RENDERER, VIEWPORT, WORLD_STATE, PALETTE, UI_CHROME) always return v1SourceLocked=1 and v2PresentationAllowed=0 regardless of V2 toggles. V2-eligible domains (PRESENTATION_MODE, TEXTURE_UPSCALE, FILTER_CONFIG, MODERN_SHAPES) require v2PresentationEnabled=1; FILTER_CONFIG additionally requires v2ConfigPersistenceEnabled=1. Source-locked against THQUEST.ASM T080/T400/T520/T560/T600/T700/T800/T900, theron_v1_track02.c, theron_v1_boot.c, theron_v1_champions.c, theron_v1_dungeon_progression.c, theron_v1_mechanics.c, theron_v1_save_load.c, theron_v1_shop.c, theron_v1_tile_renderer.c, theron_v1_viewport.c, theron_v1_world.c, theron_v1_palette.c, theron_v1_ui_chrome.c, HuC6260/HuC6270 VDC/VCE, HuC6280 CPU, ADPCM, docs/source-lock/tqr_v1_phase{0,1,2}*.md, ReDMCSB CLIKMENU/COMMAND/MOVESENS. Ctest `theron_v2_phase_gate_pc34` 220/220, headless probe `firestaff_theron_v2_phase0_v1_compatibility_lock_probe` 192/192.
- ✅ Phase 1 - V2 launch/profile separation: headless probe `firestaff_theron_v2_phase1_launch_profile_separation_probe` 52/52. Verifies launch gate (BOOT_PROFILE always V1-locked, PRESENTATION_MODE blocked when V2 off), profile gate (FILTER_CONFIG requires BOTH v2PresentationEnabled=1 AND v2ConfigPersistenceEnabled=1, with V2 off/persist on still blocked by V1 gate), Theron V1 Track 02 asset hash separation (JP Rev 1 397039af02d50d15c70b74088eb8a1cb, US ISO 3d8b78571dcd0e6eb8eb4b01eeb7fbba), cross-game hash separation (Theron Track 02 != DM1 DUNGEON.DAT d90b6b1c38fd17e41d63682f8afe5ca3341565b5f5ddae5545f0ce78754bdd85 != CSB DUNGEON.DAT 6695d2acebce49f95db1d8f3a5c733de), V1-only default behaviour, headless-safety (no game data files loaded). Ctest `theron_v2_phase1_launch_profile_separation` 1/1.
- 🔧 Phase 2 - Enhanced asset pipeline: presentation-mode selection API + filter config + V2.1 EPX upscaler pipeline are wired (`theron_v2_texture_upscale_pc34.c` provides `theron_v2_epx_upscale` indexed→RGBA via PCE palette). **2026-06-19 Theron V2.2 modern-asset module landed:** new `theron_v22_modern_assets_pc34.c/.h` mirrors dm1/csb/nexus/dm2 modules with Theron paths (`~/.firestaff/assets/theron/modern/`) and PCE source-locks (THQUEST.ASM T400/T520/T600 + HuC6260/HuC6270 VDC + HuC6270 VCE). Ctest `test_theron_v22_modern_assets_pc34` 33/33. **2026-06-19 Theron V2.2 first-cut asset pack landed:** `.openclaw/tmp/theron_v22_asset_author.py` (5 PNGs + manifest v1.0.0). Smoke: `theron_v22_modern_assets_available()=1` end-to-end. Remaining: real PBR hero art for Theron via gpt-image-2 batch + per-cell modern-art swap in Theron V1 draw pipeline (T400/T600).
- ❌ Phase 3 - Enhanced UI overlays.
- ❌ Phase 4 - Enhanced lighting/effects.
- ❌ Phase 5 - Smooth movement and viewport interpolation.
- ❌ Phase 6 - Touch/controller ergonomics.
- ❌ Phase 7 - V2 verification suite.

## Cross-Cutting
## Cross-Cutting

- 🐛 2026-06-16 firestaff_m10 unresolved symbol `_G2157_`: `image_backend_pc34_compat.c` (F0687/F0688/IMG3_*) uses `unsigned int16_t G2157_` but the definition lives in `image_frontend_pc34.c` which is NOT in `firestaff_m10` lib source list (CMakeLists.txt:56). Discovered while writing F0376_COMMAND_IsPointInBox regression test. Fix: add `src/shared/image_frontend_pc34.c` to `firestaff_m10` lib source list, or move G2157_ definition into a shared header. This is a pre-existing module decomposition issue, not introduced by the audit. See `docs/audits/REDMSB_FIRESTAFF_AUDIT_2026-06-16.md` Section "Bug B".
### Launcher and Settings

- 🔧 Start-menu feature hardening: first-pass persistence exists for quick resume, minimap, automap, combat log, soundtrack, ambient audio, UI scale, streamer mode, custom music, custom dungeon, screenshot path, and all five per-game option slots; remaining work is polished UI flow, runtime handoff for every option, save export/import, session timer, manual/docs launcher, cloud sync, and Custom/V2 smooth-turn-pan toggles.

### Touch and Controller Support

- ❌ Gesture navigation for runtime movement and turning.
- ❌ UI scaling and touch-target audit across launcher and game views.

### Accessibility

- ❌ Screen reader integration for launcher and game-critical state.
- 🔧 High-contrast presentation hardening: launcher output is remapped to a restricted high-contrast palette; remaining work is in-game overlay coverage.
- 🔧 Configurable font sizing hardening: launcher `fontScale` affects M12 text rendering; remaining work is in-game overlays and UI-fit coverage.

### Build and CI Health

- 🔧 Linker "ignoring duplicate libraries" warnings: target-link-order artifact, not a code issue, but worth a one-line fix to silence the link-time noise.
- ✅ Pre-existing ctest parity-evidence failures (now all green as of 2026-06-17, sweep extended 2026-06-18): the 4 previously-blocked chained gates (pass373/374/508/512) and 3 downstream gates (pass623/625/626) all now pass. **All 508/508 ctest targets green (2026-06-18, +11 over 497/497: 5 chest tests + 1 chest probe + pass784 binary/verifier + pass785 binary/verifier + 3 draw_order probes).** The fix path: watchdog updated `tools/verify_pass373_dm1_v1_launcher_viewport_redraw_wall_occlusion_path.py` to use `enter,down,down,down,down,down,down,enter,turn-right` script (was `right`), which lets the existing turn-right script token flow through the new `M12_MENU_INPUT_TURN_RIGHT` keyboard-rework route. 3 verify scripts then had line-range updates (verify_pass623, verify_v1_status_refresh_order, plus the m11_game_view control-strip arrow zones + test_m11_overlay_command_queue_block keyboard test). pass508/512 now use `set_tests_properties(... DEPENDS pass373;pass374)` so ctest -j4 runs them in the right order. The 8 `if(EXISTS "probes/...")` guards in CMakeLists were switched to `${CMAKE_CURRENT_SOURCE_DIR}/probes/...` so 3 evidence probes (draw_order + game_loop_redraw_cadence + palette_as_before) now build and run from subdir build dirs. Note: `nexus_v2_lighting` was previously listed as having a broken `add_test` entry; this has been fixed (the binary exists, ctest #472 + #473 pass).
- ✅ CSB V2 lib link error (resolved 2026-06-17 by auto-fixer): was defined as TODO but now resolved — `csb_v2_vfx_fire_projectile` is properly defined in `src/csb/csb_v2_vfx_particles.c` and linked into the affected test executables. `test_csb_v2_smooth_runtime_binding`, `test_csb_v2_lighting_dynamic`, `test_csb_v2_phase7_verification` all green.
- ✅ Theron V1 lib link errors (resolved 2026-06-17): was defined as TODO but now resolved — `src/theron/theron_v1_compat.c` provides compat shims for all combat symbols declared in `include/theron_v1_combat.h`. New `firestaff_theron_v1_mechanics_champions_probe` (68/68) now active.
- 🔧 Watchdog parity-evidence manifests: parity-evidence files are refreshed by automated watchdog passes on every regression run. Manifests may report transient `FAIL` on gates whose line number has shifted (see the line-drift bullets above) or where a recent change has altered the test binary output; verify against the current source before treating any one FAIL as a real regression.
- ✅ Lefthook CI hook: `.github/workflows/verify.yml` installs Go, installs `lefthook`, adds `$(go env GOPATH)/bin` to PATH, and runs `lefthook run ci` in the asset-hygiene job. Local dev machines can still no-op gracefully when Lefthook is absent.

## Known Bugs

- 🐛 Viewport/collision reports without capture manifests must stay as bugs until paired original PC 3.4 evidence or a reproducible local probe exists.

- ✅ 2026-06-18 pass792 DM1 V1 steal-from-slot-indices: new `dm1_v1_steal_from_slot_indices_pc34_compat` module (include + src) + test (96/96) + Python verifier + ctest entry pin the contract that Graphics.dat item 562 init data `G0025_auc_Graphic562_StealFromSlotIndices[8]` is `{NECK=10, POUCH_1=11, BACKPACK_LINE1_1=13, QUIVER_LINE1_1=12, NECK=10, BACKPACK_LINE1_1=13, POUCH_2=6, BACKPACK_LINE1_1=13}` and that GROUP.C F0193 (StealFromChampion) dispatches it with `counter = RANDOM(8)`, `lookup G0025[counter]`, `if (== C13_SLOT_BACKPACK_LINE1_1) += RANDOM(17)`, and counter loops with `++Counter &= 0x0007`. Source-locked against DATA.C:31/244-251 + GROUP.C:1032/1041/1045/1075 + DEFS.H:786/790-793. Disjoint from pass784-791 (mirror-candidate C040 + wound + ammunition-compat). 565/565 ctest green when sequential (was 561/561 — pass792 adds one ctest binary + one Python verifier). CTest `dm1_v1_steal_from_slot_indices_pc34_compat` 96/96, Python `pass792_dm1_v1_steal_from_slot_indices` PASS, manifest at `parity-evidence/verification/pass792_dm1_v1_steal_from_slot_indices_pc34_compat/manifest.json` + report at `parity-evidence/pass792_dm1_v1_steal_from_slot_indices_pc34_compat.md`. Note: pass791 was used by watchdog for the champion-panel ammunition-compat gate, so this gate shifted to pass792.

- ✅ 2026-06-18 pass798/800/801/802 four Graphics.dat init-table non-mirror-candidate gates landed in a single batch: four new ctest binaries + four new Python verifiers all PASS, source-locked against the G0026/G0030/G0039/G0040 graphics.dat init vars (DATA.C:32/36/45/46 declarations + DATA.C:253-260/264-309/359-360 PC 3.4 init + DATA.C:911-918/927-972/1088-1089 post-1.3 Atari init + each var's specific read sites in OBJECT.C + CHAMDRAW.C + CHAMPION.C + MENU.C + PANEL.C + TIMELINE.C). **pass798** G0026_ai_Graphic562_IconGraphicFirstIconIndex[7] = {0, 32, 64, 96, 128, 160, 192} (the 32-icon stride between graphics #42..#48; OBJECT.C:312-319 + OBJECT.C:455-467 walk G0026 in a 7-iteration loop) — 86/86 ctest + Python verifier PASS. **pass800** G0030_as_Graphic562_SlotBoxes[46] = 8 status-box hands + 30 inventory slots + 8 chest slots (SLOT_BOX { X, Y, ZoneIndex=0, IconIndex }; read sites: OBJECT.C:435/521 + CHAMDRAW.C:557/562; status-box hands have +20 within-pair +69 between-champion stride; chest slots trace a curved bottom row with Y monotonically increasing 59..105) — 565/565 ctest + Python verifier PASS. **pass801** G0039_ai_Graphic562_LightPowerToLightAmount[16] = {0, 5, 12, 24, 33, 40, 46, 51, 59, 68, 76, 82, 89, 94, 97, 100} (the torch-light power-to-amount table; read sites: PANEL.C:412 + CHAMPION.C:529/645 Illumulet delta + MENU.C:1608/1936/1941 spell paths + TIMELINE.C:1754 diff) — 126/126 ctest + Python verifier PASS. **pass802** G0040_ai_Graphic562_PaletteIndexToLightAmount[6] = {99, 75, 50, 25, 1, 0} (the dungeon palette thresholds walked top-down by PANEL.C:419-423 with `while (*p++ > TotalLightAmount) PaletteIndex++` to set G0304_i_DungeonViewPaletteIndex) — 82/82 ctest + Python verifier PASS. 587/587 ctest green when sequential (was 565/565 — +8 new test entries = 4 ctest binaries + 4 Python verifiers). All four are non-mirror-candidate contracts (seeded by the dmweb graphics.dat-item-560-562 init-var documentation link). Disjoint from pass784-799 (mirror-candidate C040 + champion-panel/leader/mirror + auto-chest + chest-open-stack-split).

- ✅ 2026-06-18 pass790 DM1 V1 wound-probability-index-to-mask: new `dm1_v1_wound_probability_index_to_mask_pc34_compat` module (include + src) + test (37/37) + Python verifier + ctest entry pin the contract that Graphics.dat item 562 init data `G0024_auc_Graphic562_WoundProbabilityIndexToWoundMask[4]` is `{FEET=0x20, LEGS=0x10, TORSO=0x08, HEAD=0x04}` in that order, and that PROJEXPL.C:1386 reads this table after a wound-test branch (WoundTest & 0x0070) while the fallback branch (PROJEXPL.C:1389) uses `MASK0x0001_WOUND_READY_HAND`. Source-locked against DATA.C:30/243 + PROJEXPL.C:1378-1389 + DEFS.H:736-741. This is the first non-mirror-candidate contract (the wound/projexpl namespace), seeded by the dmweb graphics.dat-item-560-562 init-var documentation link. Disjoint from pass784-789 (mirror-candidate C040 gates). 561/561 ctest green when sequential (was 559/559 — pass790 adds one ctest binary + one Python verifier). CTest `dm1_v1_wound_probability_index_to_mask_pc34_compat` 37/37, Python `pass790_dm1_v1_wound_probability_index_to_mask` PASS, manifest at `parity-evidence/verification/pass790_dm1_v1_wound_probability_index_to_mask_pc34_compat/manifest.json` + report at `parity-evidence/pass790_dm1_v1_wound_probability_index_to_mask_pc34_compat.md`.

- ✅ 2026-06-18 pass789 DM1 V1 mirror-candidate C040 save-game-while-panel-live: new `dm1_v1_mirror_candidate_c040_save_game_while_panel_live_pc34_compat` module (include + src) + test (43/43) + Python verifier + ctest entry pin the contract that COMMAND.C F0380:2367-2369 gates the C140 save-game command on `G0305 > 0 && !G0299`. While the C040 panel is live (G0299 set) the C140 command is dropped; after F0282(C162) clears G0299 the save fires F0433. This protects against saving a half-resolved resurrect candidate chain. Source-locked against COMMAND.C F0380:2367-2369 + STARTEND.C F0433 + REVIVE.C F0280:124-132 + REVIVE.C F0282:744-806 + DEFS.H C140/C040/M568/G0299/G0305/G0411. Disjoint from pass788 (C012..C015), pass787 (C111), pass786 (C100 with G0514), pass785 (C007..C011), and pass784 (cancel-then-reopen). 559/559 ctest green when sequential (was 524/524 — pass789 adds one ctest binary + one Python verifier; pass788 hand-wired entry was reverted by watchdog `9c588cad` due to header-mismatch so ctest total dropped 3 tests then re-stabilized with the bulk-wire foreach). CTest `dm1_v1_mirror_candidate_c040_save_game_while_panel_live_pc34_compat` 43/43, Python `pass789_dm1_v1_mirror_candidate_c040_save_game_while_panel_live` PASS, manifest at `parity-evidence/verification/pass789_dm1_v1_mirror_candidate_c040_save_game_while_panel_live_pc34_compat/manifest.json` + report at `parity-evidence/pass789_dm1_v1_mirror_candidate_c040_save_game_while_panel_live_pc34_compat.md`.

- ✅ 2026-06-18 pass788 DM1 V1 mirror-candidate C040 status-box-click-while-panel-live: new `dm1_v1_mirror_candidate_c040_status_box_click_while_panel_live_pc34_compat` module (include + src) + test (46/46) + Python verifier + ctest entry pin the contract that COMMAND.C F0380:2159-2161 gates the C012..C015 champion-status-box click on `(champion_index < G0305) && !G0299`. With party_count=2 the C012..C013 status-box clicks are in range and dropped by !G0299 (rejectedWhileLive=2); C014..C015 are out-of-range (rejectedOutOfRange=2). After F0282(C162) clears G0299 the click fires F0367. Source-locked against COMMAND.C F0380:2159-2161 + COMMAND.C F0367 + REVIVE.C F0280:124-132 + REVIVE.C F0282:744-806 + DEFS.H C012..C015/C040/M568/G0299/G0305/G0411. Disjoint from pass787 (C111), pass786 (C100 with G0514 magic-caster gate), pass785 (C007..C011 inventory-toggle), and pass784 (cancel-then-reopen same-tick). 524/524 ctest green when sequential (was 522/522 — pass788 adds one ctest binary + one Python verifier). CTest `dm1_v1_mirror_candidate_c040_status_box_click_while_panel_live_pc34_compat` 46/46, Python `pass788_dm1_v1_mirror_candidate_c040_status_box_click_while_panel_live` PASS, manifest at `parity-evidence/verification/pass788_dm1_v1_mirror_candidate_c040_status_box_click_while_panel_live_pc34_compat/manifest.json` + report at `parity-evidence/pass788_dm1_v1_mirror_candidate_c040_status_box_click_while_panel_live_pc34_compat.md`.

- ✅ 2026-06-18 pass787 DM1 V1 mirror-candidate C040 action-area-click-while-panel-live: new `dm1_v1_mirror_candidate_c040_action_area_click_while_panel_live_pc34_compat` module (include + src) + test (42/42) + Python verifier + ctest entry pin the contract that COMMAND.C F0380:2309-2311 gates the C111 action-area click on `!G0299_ui_CandidateChampionOrdinal`. Two phases: (1) panel live → 3 action clicks dropped, F0371 not called; (2) F0282(C162) clears G0299 → 1 action click fires F0371. Source-locked against COMMAND.C F0380:2309-2311 + COMMAND.C F0371 + REVIVE.C F0280:124-132 + REVIVE.C F0282:744-806 + DEFS.H C111/C040/M568/G0299/G0305/G0411. Disjoint from pass786 (which covers C100 with G0514 magic-caster gate), pass785 (C007..C011 inventory-toggle), and pass784 (cancel-then-reopen same-tick). 522/522 ctest green (was 520/520 — pass787 adds one ctest binary + one Python verifier; 3 movement-gate parallel-timing flakes on `pass374/pass508/pass512` resolve when run sequentially, not regressions). CTest `dm1_v1_mirror_candidate_c040_action_area_click_while_panel_live_pc34_compat` 42/42, Python `pass787_dm1_v1_mirror_candidate_c040_action_area_click_while_panel_live` PASS, manifest at `parity-evidence/verification/pass787_dm1_v1_mirror_candidate_c040_action_area_click_while_panel_live_pc34_compat/manifest.json` + report at `parity-evidence/pass787_dm1_v1_mirror_candidate_c040_action_area_click_while_panel_live_pc34_compat.md`.

- ✅ 2026-06-18 pass786 DM1 V1 mirror-candidate C040 spell-area-click-while-panel-live: new `dm1_v1_mirror_candidate_c040_spell_area_click_while_panel_live_pc34_compat` module (include + src) + test (48/48) + Python verifier + ctest entry pin the contract that COMMAND.C F0380:2303-2306 gates the C100 spell-area click on `!G0299_ui_CandidateChampionOrdinal && G0514_i_MagicCasterChampionIndex != CM1_CHAMPION_NONE`. Three phases: (1) panel live + magic caster valid → 3 spell clicks dropped, F0370 not called; (2) F0282(C162) clears G0299 → 1 spell click fires F0370; (3) G0514 cleared to CM1_CHAMPION_NONE → 1 more spell click dropped. Source-locked against COMMAND.C F0380:2303-2306 + COMMAND.C F0370:2482-2520 + REVIVE.C F0280:124-132 + REVIVE.C F0282:744-806 + DEFS.H C100/C040/M568/G0299/G0305/G0411/G0514. Disjoint from pass785 (which covers C007..C011 inventory-toggle without the G0514 magic-caster gate) and pass784 (cancel-then-reopen same-tick). 520/520 ctest green (was 508/508 — pass786 adds one ctest binary + one Python verifier). CTest `dm1_v1_mirror_candidate_c040_spell_area_click_while_panel_live_pc34_compat` 48/48, Python `pass786_dm1_v1_mirror_candidate_c040_spell_area_click_while_panel_live` PASS, manifest at `parity-evidence/verification/pass786_dm1_v1_mirror_candidate_c040_spell_area_click_while_panel_live_pc34_compat/manifest.json` + report at `parity-evidence/pass786_dm1_v1_mirror_candidate_c040_spell_area_click_while_panel_live_pc34_compat.md`.

- ✅ 2026-06-17 pass785 DM1 V1 mirror-candidate C040 inventory-toggle-while-panel-live: new `dm1_v1_mirror_candidate_c040_inventory_toggle_while_panel_live_pc34_compat` module (include + src) + test (44/44) + Python verifier + ctest entry pin the contract that COMMAND.C F0380:2181-2183 gates the C007..C011 inventory-toggle commands on `!G0299_ui_CandidateChampionOrdinal`. While the C040 panel is live (G0299 set), all 5 inventory-toggle commands are dropped without calling F0355; after F0282(C162) clears G0299 the toggle becomes live again. Source-locked against COMMAND.C F0380:2181-2183 + PANEL.C F0355:2299-2318 + REVIVE.C F0280:124-132 + REVIVE.C F0282:744-806 + DEFS.H C007..C011/C040/M568/G0299/G0305/G0411. Disjoint from pass784 cancel-then-reopen-same-tick (which covers F0282+new-sensor F0280 in same tick; this gate covers F0380:2181-2183 inventory-toggle rejection). 508/508 ctest green (was 506/506 — pass785 adds one ctest binary + one Python verifier). CTest `dm1_v1_mirror_candidate_c040_inventory_toggle_while_panel_live_pc34_compat` 44/44, Python `pass785_dm1_v1_mirror_candidate_c040_inventory_toggle_while_panel_live` PASS, manifest at `parity-evidence/verification/pass785_dm1_v1_mirror_candidate_c040_inventory_toggle_while_panel_live_pc34_compat/manifest.json` + report at `parity-evidence/pass785_dm1_v1_mirror_candidate_c040_inventory_toggle_while_panel_live_pc34_compat.md`.

- ✅ 2026-06-17 pass784 DM1 V1 mirror-candidate C040 cancel-then-reopen same tick: new `dm1_v1_mirror_candidate_c040_cancel_then_reopen_same_tick_pc34_compat` module (include + src) + test (53/53) + Python verifier + ctest entry pin the contract that a single tick drives both F0282(C162 cancel) AND F0280(new C127 sensor) in order, ending with a fresh C040 panel on a fresh candidate ordinal and a fresh party count mid-tick (2 -> 1 on cancel, 1 -> 2 on reopen). Source-locked against REVIVE.C F0280:124-132 + REVIVE.C F0282:744-806 (C162 cancel branch 744-783) + PANEL.C F0355:2299-2318 + COMMAND.C F0378:1956-1990 (M568_PANEL_RESURRECT_REINCARNATE dispatch) + MOVESENS.C F0275:1502 (C127_SENSOR_WALL_CHAMPION_PORTRAIT -> F0280) + DEFS.H C040/M568/C127/C162/G0299/G0305/G0415/G0424. Disjoint from pass760 mirror-candidate chrome-after-non-candidate-transition, pass762 mirror-candidate rotate-in-progress-open, and the chest_c040_cancel_reopen_pickup gate (M569 chest path, not M568 mirror candidate). 506/506 ctest green (was 497/497 — pass784 adds one ctest binary + one Python verifier). CTest `dm1_v1_mirror_candidate_c040_cancel_then_reopen_same_tick_pc34_compat` 53/53, Python `pass784_dm1_v1_mirror_candidate_c040_cancel_then_reopen_same_tick` PASS, manifest at `parity-evidence/verification/pass784_dm1_v1_mirror_candidate_c040_cancel_then_reopen_same_tick_pc34_compat/manifest.json` + report at `parity-evidence/pass784_dm1_v1_mirror_candidate_c040_cancel_then_reopen_same_tick_pc34_compat.md`.

- ✅ 2026-06-18 c545_drop_while_panel_live header resolved: `include/dm1_v1_mirror_candidate_c545_drop_while_panel_live_pc34_compat.h` and `src/dm1/dm1_v1_mirror_candidate_c545_drop_while_panel_live_pc34_compat.h` expanded to declare `Dm1V1MirrorC545DropSpecPc34Compat`, `Dm1V1MirrorC545DropEvidencePc34Compat`, `Dm1V1MirrorC545DropResultPc34Compat`, and the full `Dm1V1MirrorC545DropStatePc34Compat`. Spec initializer order in the .c matches test expectations (leaderIndex, partyChampionCount, candidateOrdinal, partyTailChampion, leaderHandThing, previousCellThing, openChestThing, panelGraphic=C040_PANEL, panelId=M568_CANDIDATE_PANEL, c545Zone, c070Command). `DM1_V1_MirrorCandidateC545DropWhilePanelLive_RunPc34Compat(state, outResult)` signature corrected (was `(state, sequenceIndex)`). 4 macros added: `DM1_V1_MIRROR_C545_DROP_C040_PANEL_PC34_COMPAT=568`, `M568_CANDIDATE_PANEL_PC34_COMPAT=5694`, `C545_ZONE_PC34_COMPAT=0xC5`, `C070_MOUTH_PC34_COMPAT=0x70`, plus `SLOT_COUNT_PC34_COMPAT=8` + `NONE_PC34_COMPAT=0xffffu`. CTest `dm1_v1_mirror_candidate_c545_drop_while_panel_live_pc34_compat` 166/166 PASS. 569/569 ctest green when sequential.

- ✅ 2026-06-19 pass841 FTL swoosh 4bpp-packed buffer fix (user-reported v2.9.0 macOS arm64 regression). Root cause: `SWSH_Compat_ExpandLogoToBitmap` writes Atari ST low-res 4bpp-packed (32000 bytes, 2 pixels/byte) into a 64000-byte buffer the runtime treats as 1-byte-per-pixel indexed, so the FTL logo rendered only in the left half with vertical stripes. Fix: new helper `m11_swsh_unpack_4bpp_to_indexed` in `src/engine/main_loop_m11.c` (mirrors the existing `m11_unpack_title_4bpp_to_indexed` used by the DM title path); dedicated 32000-byte packed buffer for SWSH expansion, then unpack into 64000-byte indexed buffer before palette/RGBA conversion. Also removed dead `paletteDirty` flag in `m11_play_ftl_swoosh_if_available` (was initialized to 0, never set to 1, so the WAIT_VBLANKS `if (paletteDirty) render` branch was unreachable). Source-lock against ReDMCSB Atari ST VDI / LINEA F0080_vq_extend / F0086_vs_clip low-res Physbase layout. Verification: new `probes/v1/firestaff_v1_ftl_swoosh_palette_regression_probe.c` (pass841) — 9/9 invariants incl. F841_SWSH_BUG_PACKED_DETECTED + F841_SWSH_UNPACK_VALID + F841_SWSH_UNPACK_COVERAGE + 4 load-bearing palette invariants; pre-fix sha256 `56549f3a...` post-fix `6612c1b1...`. New ASCII companion `probes/v1/firestaff_v1_ftl_swoosh_ascii_probe.c` dumps the unpacked logo for eyeball regression check. CTest: `ftl_swoosh_palette_regression` + `ftl_swoosh_ascii_sanity` PASS, full suite 698/698 green.
- ✅ 2026-06-20 pass897+ Apple-Silicon DM title-palette Metal-path probe (sibling to pass842 above): new `probes/v1/firestaff_v1_dm_title_palette_silicon_probe.c` skips with PASS on non-Apple-Silicon hosts and runs on Mac mini / MacBook Pro M-series. Walks the C12_PRESENTS / C13_DUNGEON + C14_MASTER switch via `M11_Render_PresentIndexedWithSpecialPalette`, then `SDL_RenderReadPixels` + `SDL_ReadSurfacePixel` for the GPU-side RGBA readback, byte-by-byte compared against `F9011_VGA_GetSpecialColorRgb_Compat` (the CPU-side contract). Source-lock: same `vga_palette_pc34_compat.h` that pass841 (FTL swoosh 4bpp) + pass842 (TITLE regression) already source-locked. New public APIs `M11_Render_GetWindow()` + `M11_Render_GetRenderer()` in `include/render_sdl_m11.h` + `src/engine/render_sdl_m11.c` let probes + test harnesses reach the SDL handles for readback. CTest `dm_title_palette_silicon` PASS (5/5 invariants: AS_TITLE_PALETTE_SLOT_{0,1,2,3} + AS_DEFAULT_PALETTE_BRIGHTNESS_0). `verify_pass897_dm1_v1_dm_title_palette_silicon_pc34_compat` Python verifier PASS. Probe-binary gitignore entry for parity-evidence added. Companion probe for the V2.2 modern-renderer path below.
- ✅ 2026-06-20 pass898+ Apple-Silicon DM1 V2.2 modern-renderer Metal-path probe: `probes/v1/firestaff_v1_dm_v22_modern_renderer_silicon_probe.c` skips with PASS on non-Apple-Silicon hosts. Walks the V22 modern-art render path end-to-end on Apple Silicon SDL3/Metal: `dm1_v2_presentation_mode_set(DM1_V2_PM_V22_MODERN)` + `dm1_v2_presentation_mode_set_modern_pack_available(1)` -> `m11_v22_shape_cache_update(...)` -> `m11_v22_render_overlay(fb, 320, 200)` -> `M11_Render_PresentIndexed(fb, 320, 200)` -> `SDL_RenderReadPixels` + `SDL_ReadSurfacePixel` for GPU readback of the V22 overlay. Updated 2026-06-21 to cover 6 invariants: V1 inactive no-op, V22 paints 9 cells, GPU readback bright, all 4 direction values paint 4x9 cells with non-zero CPU/GPU samples, `dm1_v2_shape_runtime_composition` reports the full 12-cell V1/V22 inactive/active split, and V22 -> V1 transition leaves the framebuffer unchanged. Disjoint from pass897+ (V1 TITLE palette). Source-lock: same dm1_v2_presentation_mode_pc34 + m11_v22_shape_cache_pc34 + m11_v22_render_overlay_pc34 + dm1_v2_shape_runtime_pc34 contract that the CPU-side tests source-lock. CTest `dm_v22_modern_renderer_silicon` PASS (6/6). Reuses the `M11_Render_GetWindow()` + `M11_Render_GetRenderer()` accessors from pass897+. Catches Metal/Retina GPU bugs that pass842 CPU-only / pass897+ V1 palette probes miss (V22 overlay placeholder palette, V22 framebuffer coordinates, V22 present path, direction-specific cache activation).
- 🐛 DM1 v2.9.0 (DMG Firestaff-2.9.0-macos-arm64.dmg, 2026-06-19) Dungeon Master title animation wrong palette on Apple Silicon (Mac Mini + MacBook Pro, both arm64). Sibling to pass841 (FTL swoosh fix above). v2.7.11 title-palette fixes (`970d5607`, `09091de4`, `38d0b76b`) are present in v2.9.0, so not a recent-source regression. The title path uses `m11_unpack_title_4bpp_to_indexed` correctly, but `m11_play_redmcsb_title_intro_if_available` may still have a palette-selection issue when the GRAPHICS.DAT C001 graphic is the TITLE.DAT fallback path (currently the only path in tests since C001 is not always available). Probes now cover this fully on Apple Silicon: pass842 (CPU side via V1_TitleFrontend_*) + pass897+ (GPU side via M11_Render_PresentIndexedWithSpecialPalette).

- 🔧 Next: DM1 V2 material/pixel gates. DM1 V20/V21/V22 Apple-Silicon renderer readbacks are now covered by `dm_v20_filtered_renderer_silicon`, `dm_v21_upscale_renderer_silicon`, and `dm_v22_modern_renderer_silicon`; DM1 and CSB V2.2 in-place render paths both have cache-independent CTest probes (`dm1_v22_inplace_render_probe`, `csb_v22_inplace_render_probe`). Remaining follow-up gates: (a) finished DM1 V2.2 in-place draw material/pixel verification once `m11_draw_dm1_*` consumes modern art instead of the placeholder overlay; (b) later CSB real-asset pixel/material verification once the 9-square runtime draw passes consume finished modern art.

- 🔧 Pool / heartbeat maintenance: 10 watchdog pool jobs at `/private/tmp/firestaff-pool-jobs/` are active (worker pids 14365-14368 currently running pool-probes). All 10 jobs from 2026-06-19 17:41 refill batch; workers successfully consume them in ~30-60s/probe. Refill script at `/tmp/firestaff-refill.sh` keeps the pool at 10 per HEARTBEAT.md. No refill needed unless the pool drops below 10 (e.g. workers dying mid-job — the existing pool-refill script doesn't auto-detect stuck jobs, but they get consumed correctly here).
