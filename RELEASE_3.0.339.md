# Firestaff v3.0.339

## Developer changes

- `CSB Amiga capture harness`: add an explicitly opt-in writable private save
  disk mode, preserving read-only original media as the default capture path.

- `SPDX source SBOM`: update the deterministic source inventory for this
  release version while continuing to exclude game data, firmware, saves,
  captures, and local paths.
